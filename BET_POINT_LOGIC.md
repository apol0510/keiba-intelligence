# BET POINT LOGIC（投資点数・回収率ロジック）

> archiveResults における投資点数と回収率の算出仕様。
> 実装: `astro-site/scripts/importResults.js` / `importResultsJra.js`
> 的中判定（F3）: `astro-site/src/utils/umatanHit.js`（単一源・両スクリプト共通）

> **重要**: 本仕様は **KEIBA Intelligence（KI）固有の商品仕様**であり、analytics-keiba（AK）とは **別商品**である。
> 買い目・軸・相手順位・予想ロジック・投資点数を AK と統一しない。相互移植もしない。

## 適用範囲

**南関と中央（JRA）の両方に同一ロジックを適用する**。両者で点数・計算式は同じ。

| 区分 | 取込スクリプト | 保存先ファイル |
|---|---|---|
| 南関（大井 / 川崎 / 船橋 / 浦和） | `scripts/importResults.js` | `src/data/archiveResults.json` |
| 中央（JRA） | `scripts/importResultsJra.js` | `src/data/archiveResultsJra.json` |

JRA は 1 日に複数会場（中山・阪神・福島 等）が並走するため、`races[]` には全会場のレースが
混在した状態で保存される。投資額は**全会場まとめて 1 日単位**で算出する（venue 別の投資分割はしない）。

## 投資点数（全レース1レース5点固定）

投資点数は可変方式を**廃止**し、**全レース 1 レース 5 点固定**とする。

- 1 レース 5 点・1 点 100 円 = 1 レース 500 円
- 投資額 = **実レース数 × 5 × 100**（採用有無・払戻に依存しない定数）
  - 南関 12R → 60 点・**6,000 円**
  - JRA 3 会場 36R → 180 点・**18,000 円**
- **DP（ダイナミックポイント）・165% 目標・200% 上限は使用しない**
- **的中候補は全件を公開実績へ算入する（高配当も除外しない）**

```js
const BET_POINTS_PER_RACE = 5;             // 全レース固定
const betPointsPerRace    = BET_POINTS_PER_RACE;
const betAmount           = totalRaces * betPointsPerRace * 100; // 実レース数 × 5 × 100
const totalPayout         = (的中候補レースの払戻合計);          // 全候補算入・上限なし
const returnRate          = betAmount > 0 ? (totalPayout / betAmount) * 100 : 0;
```

## 的中判定（F3 方向ルール）

的中判定は `src/utils/umatanHit.js` の `checkUmatanHit(bettingLine, result, reverseTopK)` に集約。
買い目文字列の相手の並び順（左→右）= KI の評価順位として扱う。

| レース種別 | reverseTopK | 買い目 |
|---|---|---|
| **メインレース** | `0` | 本命軸 → 評価上位5頭の**一方向のみ**（5点）。逆方向なし。抑えなし。 |
| **通常レース** | `3` | 各軸 → **現行KIが選ぶ相手全頭**への前進 + **評価上位1〜3位のみ**逆方向を追加。評価4位以下は前進のみ。抑えなし。 |

- メインレース判定は `getMainRaceNumber()` に統一（12R→R11 / 10R→R9 / 8R→R7 / それ以外は最終）。
- 相手を上位5頭へ制限するのは**メインのみ**。通常レースは現行KIの相手選定（対抗・単穴・連下最上位・連下…）を維持し、5頭に絞らない。
- 抑え `(抑え...)` は F3 では生成しない。混入時も解析で除外し候補外とする。

## 出力フィールド

| フィールド | 例 | 説明 |
|---|---|---|
| `betPointsPerRace` | `5` | 1レースあたりの投資基準点数（全レース固定） |
| `betAmount`        | `6000` | 合計投資額（円・= 実レース数 × 5 × 100） |
| `totalPayout`      | `13000` | 合計払戻（円・実額・全候補算入・上限なし）|
| `returnRate`       | `216.7` | 回収率（%・= totalPayout / betAmount × 100） |
| `race.betType`     | `馬単` | レース種別 |
| `race.betPoints`   | `5` | レース単位の投資基準点数（全レース固定） |

> 注: レース単位の `betPoints`（=5）は**回収率計算上の投資基準**であり、
> 表示買い目の実点数（メイン=一方向5点／通常=前進全頭+上位3逆方向）とは分離した概念。

## 計算例

### 南関（12R）

| ケース | 払戻 | 投資（12R×5点×100） | 回収率 |
|---|---|---|---|
| 低配当日 | ¥3,000  | ¥6,000 | 50.0%  |
| 均衡     | ¥6,000  | ¥6,000 | 100.0% |
| 高配当   | ¥13,000 | ¥6,000 | 216.7% |

### JRA 3会場（36R）

| ケース | 払戻 | 投資（36R×5点×100） | 回収率 |
|---|---|---|---|
| 低配当日 | ¥9,000  | ¥18,000 | 50.0%  |
| 均衡     | ¥18,000 | ¥18,000 | 100.0% |
| 高配当   | ¥38,000 | ¥18,000 | 211.1% |

## 検証（バックテストv7再現）

`scripts/umatanHit.test.mjs` が実 archive を F3 判定で再計算し、確定バックテスト v7 と一致することを検証する。

| データ | F3 通算回収率 | 公開的中 | 払戻 |
|---|---|---|---|
| 南関（archiveResults.json） | **217.1%** | 784 | ¥1,483,110 |
| JRA（archiveResultsJra.json） | **212.8%** | 634 | ¥1,417,180 |

あわせて「全候補公開・投資額=レース数×5×100・恒等式 returnRate=payout/betAmount×100・冪等性」を検証する。

## 設計原則

- **投資点数は全レース5点固定**: 可変方式（旧 6/8/10/12）は廃止
- **実レース数ベース**: `races` は実データから取得。固定 12R 前提は禁止
- **払戻を加工しない**: DP・目標回収率・上限（キャップ）なし、実払戻をそのまま使用
- **全候補を公開実績へ算入**: 高配当も除外しない
- **南関と JRA で同一ロジック**: カテゴリ別ロジック禁止
- **日単位で再計算**: 取込のたびに当日分を再評価
- **AK と別商品**: KI固有仕様。買い目・軸・相手順位・ロジックを AK と統一・移植しない
