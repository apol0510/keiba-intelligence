---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { readdirSync, existsSync, statSync } from 'fs';
import { join } from 'path';

export const prerender = false;

// 南関の最新予想日付
function readLatestNankanDate(): string | null {
  const dir = join(process.cwd(), 'src', 'data', 'predictions');
  if (!existsSync(dir)) return null;
  try {
    const files = readdirSync(dir)
      .filter((name) => /^\d{4}-\d{2}-\d{2}/.test(name) && name.endsWith('.json'))
      .sort()
      .reverse();
    if (files.length === 0) return null;
    const m = files[0].match(/^(\d{4}-\d{2}-\d{2})/);
    return m ? m[1] : null;
  } catch {
    return null;
  }
}

// JRAの最新予想日付（jra/YYYY/MM/YYYY-MM-DD.json）
function readLatestJraDate(): string | null {
  const root = join(process.cwd(), 'src', 'data', 'predictions', 'jra');
  if (!existsSync(root)) return null;
  try {
    const years = readdirSync(root).filter((n) => /^\d{4}$/.test(n)).sort().reverse();
    for (const y of years) {
      const yearDir = join(root, y);
      if (!statSync(yearDir).isDirectory()) continue;
      const months = readdirSync(yearDir).filter((n) => /^\d{2}$/.test(n)).sort().reverse();
      for (const m of months) {
        const monthDir = join(yearDir, m);
        if (!statSync(monthDir).isDirectory()) continue;
        const files = readdirSync(monthDir)
          .filter((n) => /^\d{4}-\d{2}-\d{2}/.test(n) && n.endsWith('.json'))
          .sort()
          .reverse();
        if (files.length > 0) {
          const match = files[0].match(/^(\d{4}-\d{2}-\d{2})/);
          if (match) return match[1];
        }
      }
    }
    return null;
  } catch {
    return null;
  }
}

const nankanLatest = readLatestNankanDate();
const jraLatest = readLatestJraDate();

const formatDate = (date: string | null): string => {
  if (!date) return '';
  const [, m, d] = date.split('-');
  return `${parseInt(m, 10)}月${parseInt(d, 10)}日`;
};

const pageTitle = '無料AI予想';
const pageDescription = '中央競馬（JRA）と南関競馬の無料AI予想。XGBoost・LSTMによる高精度予想を毎日無料公開。';
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <div class="free-prediction-hub">
    <div class="page-header">
      <h1 class="page-title">🎁 無料AI予想</h1>
      <p class="page-subtitle">中央競馬（JRA）と南関競馬の無料予想を毎日公開</p>
      <p class="page-description">
        XGBoost・LSTM・ニューラルネットワークによる高精度AI予想。<br>
        どちらのカテゴリの予想を見ますか？
      </p>
    </div>

    <div class="category-grid">
      <div class="category-card jra">
        <div class="card-header">
          <span class="card-icon">🏇</span>
          <span class="card-title">中央競馬（JRA）</span>
        </div>
        <p class="card-summary">
          東京・中山・京都・阪神を中心とするJRA全10場の無料予想。重賞レースを含む全レースを毎週末公開。
        </p>
        <div class="card-venues">
          <span class="venue-tag">東京</span>
          <span class="venue-tag">中山</span>
          <span class="venue-tag">京都</span>
          <span class="venue-tag">阪神</span>
          <span class="venue-tag">中京・新潟・福島・小倉・札幌・函館</span>
        </div>
        {jraLatest && (
          <span class="latest-badge">📅 最新: {formatDate(jraLatest)}の予想を公開中</span>
        )}
        <div class="card-actions">
          <a href="/free-prediction/jra/" class="btn-primary">🎯 JRA無料予想を見る</a>
        </div>
      </div>

      <div class="category-card nankan">
        <div class="card-header">
          <span class="card-icon">🐎</span>
          <span class="card-title">南関競馬</span>
        </div>
        <p class="card-summary">
          大井・川崎・船橋・浦和の南関4場の無料予想。平日夜のナイター開催を含む全レースを毎日公開。
        </p>
        <div class="card-venues">
          <span class="venue-tag">大井</span>
          <span class="venue-tag">川崎</span>
          <span class="venue-tag">船橋</span>
          <span class="venue-tag">浦和</span>
        </div>
        {nankanLatest && (
          <span class="latest-badge">📅 最新: {formatDate(nankanLatest)}の予想を公開中</span>
        )}
        <div class="card-actions">
          <a href="/free-prediction/nankan/" class="btn-primary">🎯 南関無料予想を見る</a>
        </div>
      </div>
    </div>

    <div class="back-link">
      <a href="/">← トップページ</a>
      <a href="/archive/">的中実績アーカイブ</a>
      <a href="/pricing/">料金プラン</a>
    </div>
  </div>
</BaseLayout>

<style>
  .free-prediction-hub { padding: 2rem 0 3rem; color: #e2e8f0; }

  .page-header {
    text-align: center;
    margin-bottom: 3rem;
    padding: 2.5rem 2rem;
    background: rgba(30, 41, 59, 0.5);
    border-radius: 16px;
    border: 1px solid rgba(245, 158, 11, 0.3);
  }

  .page-title {
    font-size: 2.5rem;
    font-weight: bold;
    background: linear-gradient(135deg, #f59e0b 0%, #fbbf24 50%, #60a5fa 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin: 0 0 1rem;
  }
  .page-subtitle { font-size: 1.2rem; color: #cbd5e1; margin-bottom: 0.5rem; }
  .page-description { font-size: 1rem; color: #94a3b8; max-width: 640px; margin: 0 auto; line-height: 1.7; }

  .category-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(360px, 1fr));
    gap: 2rem;
    margin-bottom: 3rem;
  }

  .category-card {
    background: rgba(30, 41, 59, 0.8);
    border: 1px solid rgba(100, 116, 139, 0.3);
    border-radius: 16px;
    padding: 2.5rem;
    transition: all 0.3s;
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
  }

  .category-card.jra { border-color: rgba(59, 130, 246, 0.35); }
  .category-card.jra:hover { transform: translateY(-4px); border-color: rgba(59, 130, 246, 0.6); box-shadow: 0 12px 30px rgba(59, 130, 246, 0.3); }
  .category-card.nankan { border-color: rgba(245, 158, 11, 0.35); }
  .category-card.nankan:hover { transform: translateY(-4px); border-color: rgba(245, 158, 11, 0.6); box-shadow: 0 12px 30px rgba(245, 158, 11, 0.3); }

  .card-header { display: flex; align-items: center; gap: 0.75rem; }
  .card-icon { font-size: 2.25rem; }
  .card-title { font-size: 1.75rem; font-weight: 800; }
  .category-card.jra .card-title { color: #60a5fa; }
  .category-card.nankan .card-title { color: #fbbf24; }

  .card-summary { color: #cbd5e1; font-size: 1rem; line-height: 1.7; }
  .card-venues { display: flex; flex-wrap: wrap; gap: 0.5rem; }
  .venue-tag {
    font-size: 0.85rem;
    padding: 0.35rem 0.75rem;
    border-radius: 999px;
    background: rgba(15, 23, 42, 0.6);
    color: #cbd5e1;
    border: 1px solid rgba(100, 116, 139, 0.4);
  }

  .latest-badge {
    display: inline-block;
    align-self: flex-start;
    padding: 0.4rem 0.9rem;
    border-radius: 999px;
    font-size: 0.85rem;
    font-weight: 600;
    background: rgba(16, 185, 129, 0.15);
    color: #34d399;
    border: 1px solid rgba(16, 185, 129, 0.35);
  }

  .card-actions { display: flex; flex-direction: column; gap: 0.6rem; margin-top: auto; padding-top: 0.5rem; }

  .btn-primary {
    display: inline-block;
    text-align: center;
    padding: 1rem 1.5rem;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 700;
    font-size: 1.05rem;
    transition: all 0.3s;
  }

  .category-card.jra .btn-primary { background: linear-gradient(135deg, #3b82f6, #2563eb); color: #fff; }
  .category-card.jra .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 8px 20px rgba(59, 130, 246, 0.4); }
  .category-card.nankan .btn-primary { background: linear-gradient(135deg, #f59e0b, #d97706); color: #fff; }
  .category-card.nankan .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 8px 20px rgba(245, 158, 11, 0.4); }

  .back-link { text-align: center; margin-top: 2rem; padding-top: 2rem; border-top: 1px solid rgba(100, 116, 139, 0.3); }
  .back-link a { color: #60a5fa; text-decoration: none; font-size: 1rem; margin: 0 0.5rem; }
  .back-link a:hover { color: #3b82f6; }

  @media (max-width: 768px) {
    .page-title { font-size: 1.8rem; }
    .category-grid { grid-template-columns: 1fr; gap: 1.5rem; }
    .card-title { font-size: 1.5rem; }
  }
</style>
