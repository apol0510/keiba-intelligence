---
import BaseLayout from '../../../../layouts/BaseLayout.astro';

export const prerender = false;

const { year, month, day } = Astro.params;
const date = `${year}-${month}-${day}`;

// keiba-data-sharedから結果データ取得
let resultsData = null;
let archiveData = null;
let errorMessage = null;

try {
  // 結果JSONを取得
  const resultsUrl = `https://raw.githubusercontent.com/apol0510/keiba-data-shared/main/nankan/results/${year}/${month}/${date}.json`;
  const resultsResponse = await fetch(resultsUrl);

  if (!resultsResponse.ok) {
    throw new Error('結果データが見つかりません');
  }

  resultsData = await resultsResponse.json();

  // archiveResults.jsonから的中情報を取得
  const archiveUrl = `https://raw.githubusercontent.com/apol0510/keiba-intelligence/main/astro-site/src/data/archiveResults.json`;
  const archiveResponse = await fetch(archiveUrl);

  if (archiveResponse.ok) {
    const fullArchive = await archiveResponse.json();
    // 配列から該当日付を検索
    archiveData = fullArchive.find(entry => entry.date === date);
  }
} catch (error) {
  errorMessage = error.message;
}

const pageTitle = resultsData
  ? `${resultsData.date} ${resultsData.venue} 的中実績`
  : '結果データが見つかりません';

const pageDescription = archiveData
  ? `${resultsData.venue} ${archiveData.totalRaces}R開催、的中率${archiveData.hitRate}%、回収率${archiveData.returnRate}%、配当${archiveData.totalPayout.toLocaleString()}円`
  : '南関競馬の結果データ';
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <main class="archive-page">
    <div class="container">
      {errorMessage ? (
        <div class="error-section">
          <h1>⚠️ エラー</h1>
          <p>{errorMessage}</p>
          <a href="/results" class="btn-back">的中実績一覧へ戻る</a>
        </div>
      ) : resultsData && archiveData ? (
        <>
          {/* ページヘッダー */}
          <header class="page-header">
            <h1>
              <span class="date">{month}月{day}日</span>
              <span class="venue">{resultsData.venue}競馬</span>
            </h1>
          </header>

          {/* サマリー統計 */}
          <section class="summary-section">
            <div class="summary-grid">
              <div class="summary-card">
                <div class="summary-label">的中レース数</div>
                <div class="summary-value">{archiveData.hitRaces}/{archiveData.totalRaces}R</div>
              </div>
              <div class="summary-card">
                <div class="summary-label">的中率</div>
                <div class="summary-value highlight">{archiveData.hitRate}%</div>
              </div>
              <div class="summary-card">
                <div class="summary-label">回収率</div>
                <div class={`summary-value ${archiveData.returnRate >= 100 ? 'profit' : 'loss'}`}>
                  {archiveData.returnRate}%
                </div>
              </div>
              <div class="summary-card">
                <div class="summary-label">合計配当</div>
                <div class="summary-value profit">¥{archiveData.totalPayout.toLocaleString()}</div>
              </div>
            </div>
          </section>

          {/* レース結果一覧 */}
          <section class="races-section">
            <h2>📊 レース結果</h2>
            <div class="races-list">
              {resultsData.races.map((race) => {
                const archiveRace = archiveData.races.find(r => r.raceNumber === race.raceNumber);
                const isHit = archiveRace?.isHit || false;
                const payout = archiveRace?.umatan?.payout || 0;

                return (
                  <div class={`race-line ${isHit ? 'hit' : 'miss'}`}>
                    <span class="race-number">第{race.raceNumber}R</span>
                    <span class="race-name">{race.raceName}</span>
                    {isHit ? (
                      <>
                        <span class="hit-badge">✅ 的中</span>
                        {payout > 0 && (
                          <span class="payout">¥{(payout * 10).toLocaleString()}</span>
                        )}
                      </>
                    ) : (
                      <span class="miss-badge">❌ 不的中</span>
                    )}
                  </div>
                );
              })}
            </div>
          </section>

          {/* フッターナビ */}
          <footer class="page-footer">
            <a href="/results" class="btn-back">← 的中実績一覧へ戻る</a>
          </footer>
        </>
      ) : (
        <div class="loading">読み込み中...</div>
      )}
    </div>
  </main>
</BaseLayout>

<style lang="scss">
  .archive-page {
    min-height: 100vh;
    padding: var(--spacing-2xl) 0;
    background: var(--bg-primary);
  }

  .container {
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 var(--spacing-lg);
  }

  /* エラーセクション */
  .error-section {
    text-align: center;
    padding: var(--spacing-2xl);
    background: var(--bg-secondary);
    border-radius: var(--radius-lg);

    h1 {
      font-size: 2rem;
      margin-bottom: var(--spacing-md);
      color: var(--text-primary);
    }

    p {
      color: var(--text-secondary);
      margin-bottom: var(--spacing-xl);
    }
  }

  /* ページヘッダー */
  .page-header {
    margin-bottom: var(--spacing-2xl);

    h1 {
      font-size: 2rem;
      color: var(--text-primary);
      display: flex;
      align-items: baseline;
      gap: var(--spacing-md);

      .date {
        font-weight: 800;
        background: var(--primary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .venue {
        font-weight: 600;
        color: var(--text-secondary);
      }
    }
  }

  /* サマリーセクション */
  .summary-section {
    margin-bottom: var(--spacing-2xl);
  }

  .summary-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--spacing-lg);
  }

  .summary-card {
    background: var(--bg-secondary);
    padding: var(--spacing-xl);
    border-radius: var(--radius-lg);
    border: 1px solid var(--border-color);
    text-align: center;

    .summary-label {
      font-size: 0.875rem;
      color: var(--text-secondary);
      margin-bottom: var(--spacing-sm);
      font-weight: 500;
    }

    .summary-value {
      font-size: 2rem;
      font-weight: 800;
      color: var(--text-primary);

      &.highlight {
        color: #60a5fa;
      }

      &.profit {
        color: #10b981;
      }

      &.loss {
        color: #ef4444;
      }
    }
  }

  /* レースセクション */
  .races-section {
    h2 {
      font-size: 1.5rem;
      color: var(--text-primary);
      margin-bottom: var(--spacing-xl);
      font-weight: 700;
    }
  }

  .races-list {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-sm);
  }

  /* レース1行表示 */
  .race-line {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    padding: var(--spacing-md) var(--spacing-lg);
    background: var(--bg-secondary);
    border-radius: var(--radius-md);
    border: 1px solid var(--border-color);
    transition: all var(--transition-normal);

    &.hit {
      background: linear-gradient(90deg, rgba(16, 185, 129, 0.1) 0%, var(--bg-secondary) 100%);
      border-color: rgba(16, 185, 129, 0.3);
    }

    &:hover {
      background: var(--bg-tertiary);
    }

    .race-number {
      font-weight: 700;
      color: #60a5fa;
      min-width: 50px;
    }

    .race-name {
      flex: 1;
      color: var(--text-secondary);
      font-size: 0.9375rem;
    }

    .hit-badge {
      color: #10b981;
      font-weight: 700;
      font-size: 0.9375rem;
    }

    .miss-badge {
      color: var(--text-tertiary);
      font-weight: 600;
      font-size: 0.9375rem;
    }

    .payout {
      font-weight: 800;
      color: #10b981;
      font-size: 1.125rem;
      margin-left: var(--spacing-sm);
    }
  }

  /* フッター */
  .page-footer {
    margin-top: var(--spacing-2xl);
    padding-top: var(--spacing-2xl);
    border-top: 1px solid var(--border-color);
    text-align: center;
  }

  .btn-back {
    display: inline-block;
    padding: var(--spacing-md) var(--spacing-xl);
    background: var(--bg-secondary);
    color: var(--text-primary);
    text-decoration: none;
    border-radius: var(--radius-md);
    border: 1px solid var(--border-color);
    font-weight: 600;
    transition: all var(--transition-normal);

    &:hover {
      background: var(--bg-tertiary);
      transform: translateY(-2px);
    }
  }

  /* レスポンシブ */
  @media (max-width: 768px) {
    .page-header h1 {
      font-size: 1.5rem;
      flex-direction: column;
      gap: var(--spacing-xs);
    }

    .summary-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .summary-card .summary-value {
      font-size: 1.5rem;
    }

    .race-line {
      flex-wrap: wrap;
      gap: var(--spacing-sm);

      .race-number {
        min-width: auto;
      }

      .race-name {
        flex: 1 1 100%;
        order: 2;
      }

      .payout {
        margin-left: 0;
      }
    }
  }

  .loading {
    text-align: center;
    padding: var(--spacing-2xl);
    color: var(--text-secondary);
  }
</style>
