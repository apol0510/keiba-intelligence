---
import BaseLayout from '../../../../layouts/BaseLayout.astro';

export async function getStaticPaths() {
  return [];
}

const { year, month, day } = Astro.params;
const date = `${year}-${month}-${day}`;

// keiba-data-sharedから結果データ取得
let resultsData = null;
let archiveData = null;
let errorMessage = null;

try {
  // 結果JSONを取得
  const resultsUrl = `https://raw.githubusercontent.com/apol0510/keiba-data-shared/main/nankan/results/${year}/${month}/${date}.json`;
  const resultsResponse = await fetch(resultsUrl);

  if (!resultsResponse.ok) {
    throw new Error('結果データが見つかりません');
  }

  resultsData = await resultsResponse.json();

  // archiveResults.jsonから的中情報を取得
  const archiveUrl = `https://raw.githubusercontent.com/apol0510/keiba-data-shared/main/nankan/archive/archiveResults.json`;
  const archiveResponse = await fetch(archiveUrl);

  if (archiveResponse.ok) {
    const fullArchive = await archiveResponse.json();
    archiveData = fullArchive?.[year]?.[month]?.[day];
  }
} catch (error) {
  errorMessage = error.message;
}

const pageTitle = resultsData
  ? `${resultsData.date} ${resultsData.venue} 結果`
  : '結果データが見つかりません';

const pageDescription = archiveData
  ? `${resultsData.venue} ${archiveData.totalRaces}R開催、的中率${Math.round((archiveData.hitRaces / archiveData.totalRaces) * 100)}%、回収率${archiveData.recoveryRate}%`
  : '南関競馬の結果データ';
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <main class="results-page">
    <div class="container">
      {errorMessage ? (
        <div class="error-message">
          <h1>⚠️ エラー</h1>
          <p>{errorMessage}</p>
          <a href="/" class="btn btn-primary">トップページへ戻る</a>
        </div>
      ) : resultsData ? (
        <>
          {/* ヘッダー */}
          <section class="results-header">
            <div class="header-content">
              <h1>
                <span class="date-badge">{resultsData.date}</span>
                {resultsData.venue} 結果
              </h1>
              {archiveData && (
                <div class="stats-grid">
                  <div class="stat-card">
                    <div class="stat-label">開催レース数</div>
                    <div class="stat-value">{archiveData.totalRaces}R</div>
                  </div>
                  <div class="stat-card">
                    <div class="stat-label">的中レース数</div>
                    <div class="stat-value success">{archiveData.hitRaces}R</div>
                  </div>
                  <div class="stat-card">
                    <div class="stat-label">的中率</div>
                    <div class="stat-value">
                      {Math.round((archiveData.hitRaces / archiveData.totalRaces) * 100)}%
                    </div>
                  </div>
                  <div class="stat-card">
                    <div class="stat-label">回収率</div>
                    <div class={`stat-value ${archiveData.recoveryRate >= 100 ? 'success' : 'warning'}`}>
                      {archiveData.recoveryRate}%
                    </div>
                  </div>
                  <div class="stat-card">
                    <div class="stat-label">総払戻金</div>
                    <div class="stat-value">{archiveData.totalPayout.toLocaleString()}円</div>
                  </div>
                  {archiveData.perfectHit && (
                    <div class="stat-card perfect-hit">
                      <div class="stat-label">🎉 全レース的中</div>
                      <div class="stat-value">パーフェクト</div>
                    </div>
                  )}
                </div>
              )}
            </div>
          </section>

          {/* レース結果一覧 */}
          <section class="races-section">
            <h2>レース結果</h2>
            <div class="races-grid">
              {resultsData.races.map((race, index) => {
                const archiveRace = archiveData?.races?.find(r => r.raceNumber === `${race.raceNumber}R`);

                return (
                  <div class="race-card">
                    {/* レースヘッダー */}
                    <div class="race-header">
                      <div class="race-number">第{race.raceNumber}R</div>
                      <div class="race-name">{race.raceName || '-'}</div>
                      {archiveRace && (
                        <div class={`hit-badge ${archiveRace.hit ? (archiveRace.hitType === 'main' ? 'hit-main' : 'hit-sub') : 'hit-miss'}`}>
                          {archiveRace.hit
                            ? (archiveRace.hitType === 'main' ? '🎯 本線的中' : '✅ 抑え的中')
                            : '❌ 不的中'
                          }
                        </div>
                      )}
                    </div>

                    {/* 着順結果 */}
                    <div class="race-results">
                      <h3>着順</h3>
                      <div class="horses-list">
                        {race.results?.slice(0, 3).map((horse, pos) => (
                          <div class={`horse-item rank-${pos + 1}`}>
                            <span class="rank">{pos + 1}着</span>
                            <span class="horse-number">{horse.number}番</span>
                            <span class="horse-name">{horse.name}</span>
                          </div>
                        ))}
                      </div>
                    </div>

                    {/* 払戻金 */}
                    <div class="payouts">
                      <h3>払戻金</h3>
                      <div class="payout-grid">
                        {race.payouts?.tansho && (
                          <div class="payout-item">
                            <span class="payout-type">単勝</span>
                            <span class="payout-value">{race.payouts.tansho.payout?.toLocaleString()}円</span>
                          </div>
                        )}
                        {race.payouts?.umatan && (
                          <div class="payout-item highlight">
                            <span class="payout-type">馬単 ({race.payouts.umatan.combination})</span>
                            <span class="payout-value">{race.payouts.umatan.payout?.toLocaleString()}円</span>
                          </div>
                        )}
                        {race.payouts?.sanrenpuku && (
                          <div class="payout-item">
                            <span class="payout-type">三連複</span>
                            <span class="payout-value">{race.payouts.sanrenpuku.payout?.toLocaleString()}円</span>
                          </div>
                        )}
                        {race.payouts?.sanrentan && (
                          <div class="payout-item">
                            <span class="payout-type">三連単</span>
                            <span class="payout-value">{race.payouts.sanrentan.payout?.toLocaleString()}円</span>
                          </div>
                        )}
                      </div>
                    </div>

                    {/* 的中情報 */}
                    {archiveRace && (
                      <div class="hit-info">
                        <div class="hit-info-row">
                          <span>購入点数: {archiveRace.betPoints}点</span>
                          {archiveRace.hit && (
                            <span class="payout-amount">払戻: {archiveRace.payout.toLocaleString()}円</span>
                          )}
                        </div>
                      </div>
                    )}
                  </div>
                );
              })}
            </div>
          </section>

          {/* 戻るリンク */}
          <section class="back-section">
            <a href="/" class="btn btn-secondary">トップページへ戻る</a>
          </section>
        </>
      ) : null}
    </div>
  </main>
</BaseLayout>

<style lang="scss">
  .results-page {
    min-height: 100vh;
    padding: var(--spacing-2xl) 0;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 var(--spacing-lg);
  }

  /* エラーメッセージ */
  .error-message {
    text-align: center;
    padding: var(--spacing-2xl);
    background: var(--bg-card);
    border-radius: var(--radius-xl);
    backdrop-filter: blur(10px);

    h1 {
      font-size: 2rem;
      margin-bottom: var(--spacing-lg);
      color: var(--danger);
    }

    p {
      color: var(--text-secondary);
      margin-bottom: var(--spacing-xl);
    }
  }

  /* ヘッダー */
  .results-header {
    margin-bottom: var(--spacing-2xl);

    h1 {
      font-size: 2.5rem;
      margin-bottom: var(--spacing-xl);
      background: var(--primary-gradient);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;

      @media (max-width: 768px) {
        font-size: 1.75rem;
      }
    }

    .date-badge {
      display: inline-block;
      padding: var(--spacing-sm) var(--spacing-md);
      background: var(--bg-card);
      border-radius: var(--radius-md);
      font-size: 1.25rem;
      margin-right: var(--spacing-md);
      color: var(--text-primary);
    }
  }

  /* 統計カード */
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--spacing-lg);
    margin-top: var(--spacing-xl);
  }

  .stat-card {
    background: var(--bg-card);
    padding: var(--spacing-lg);
    border-radius: var(--radius-lg);
    backdrop-filter: blur(10px);
    border: 1px solid var(--border-color);
    text-align: center;

    &.perfect-hit {
      background: linear-gradient(135deg, rgba(16, 185, 129, 0.2) 0%, rgba(16, 185, 129, 0.1) 100%);
      border-color: var(--success);
    }

    .stat-label {
      font-size: 0.875rem;
      color: var(--text-secondary);
      margin-bottom: var(--spacing-sm);
    }

    .stat-value {
      font-size: 1.75rem;
      font-weight: 700;
      color: var(--text-primary);

      &.success {
        color: var(--success);
      }

      &.warning {
        color: var(--warning);
      }
    }
  }

  /* レースセクション */
  .races-section {
    margin-bottom: var(--spacing-2xl);

    h2 {
      font-size: 1.75rem;
      margin-bottom: var(--spacing-xl);
      color: var(--text-primary);
    }
  }

  .races-grid {
    display: grid;
    gap: var(--spacing-xl);
  }

  /* レースカード */
  .race-card {
    background: var(--bg-card);
    border-radius: var(--radius-xl);
    padding: var(--spacing-xl);
    backdrop-filter: blur(10px);
    border: 1px solid var(--border-color);
    transition: all var(--transition-base);

    &:hover {
      background: var(--bg-card-hover);
      transform: translateY(-2px);
      box-shadow: var(--shadow-lg);
    }
  }

  .race-header {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-lg);
    padding-bottom: var(--spacing-md);
    border-bottom: 1px solid var(--border-color);
    flex-wrap: wrap;

    .race-number {
      font-size: 1.25rem;
      font-weight: 700;
      color: var(--primary-end);
    }

    .race-name {
      font-size: 1.125rem;
      color: var(--text-primary);
      flex: 1;
    }
  }

  /* 的中バッジ */
  .hit-badge {
    padding: var(--spacing-sm) var(--spacing-md);
    border-radius: var(--radius-md);
    font-size: 0.875rem;
    font-weight: 600;

    &.hit-main {
      background: linear-gradient(135deg, rgba(16, 185, 129, 0.3) 0%, rgba(16, 185, 129, 0.2) 100%);
      color: var(--success);
      border: 1px solid var(--success);
    }

    &.hit-sub {
      background: linear-gradient(135deg, rgba(245, 158, 11, 0.3) 0%, rgba(245, 158, 11, 0.2) 100%);
      color: var(--warning);
      border: 1px solid var(--warning);
    }

    &.hit-miss {
      background: linear-gradient(135deg, rgba(100, 116, 139, 0.3) 0%, rgba(100, 116, 139, 0.2) 100%);
      color: var(--text-tertiary);
      border: 1px solid var(--text-tertiary);
    }
  }

  /* 着順結果 */
  .race-results {
    margin-bottom: var(--spacing-lg);

    h3 {
      font-size: 1rem;
      color: var(--text-secondary);
      margin-bottom: var(--spacing-md);
    }
  }

  .horses-list {
    display: grid;
    gap: var(--spacing-sm);
  }

  .horse-item {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
    padding: var(--spacing-md);
    background: rgba(255, 255, 255, 0.05);
    border-radius: var(--radius-md);

    &.rank-1 {
      border-left: 3px solid #ffd700;
    }

    &.rank-2 {
      border-left: 3px solid #c0c0c0;
    }

    &.rank-3 {
      border-left: 3px solid #cd7f32;
    }

    .rank {
      font-weight: 600;
      color: var(--text-secondary);
      min-width: 40px;
    }

    .horse-number {
      font-weight: 700;
      color: var(--primary-end);
      min-width: 50px;
    }

    .horse-name {
      color: var(--text-primary);
    }
  }

  /* 払戻金 */
  .payouts {
    margin-bottom: var(--spacing-lg);

    h3 {
      font-size: 1rem;
      color: var(--text-secondary);
      margin-bottom: var(--spacing-md);
    }
  }

  .payout-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: var(--spacing-md);
  }

  .payout-item {
    padding: var(--spacing-md);
    background: rgba(255, 255, 255, 0.05);
    border-radius: var(--radius-md);
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xs);

    &.highlight {
      background: rgba(59, 130, 246, 0.15);
      border: 1px solid var(--primary-end);
    }

    .payout-type {
      font-size: 0.875rem;
      color: var(--text-secondary);
    }

    .payout-value {
      font-size: 1.125rem;
      font-weight: 700;
      color: var(--text-primary);
    }
  }

  /* 的中情報 */
  .hit-info {
    padding-top: var(--spacing-md);
    border-top: 1px solid var(--border-color);

    .hit-info-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      font-size: 0.875rem;
      color: var(--text-secondary);

      .payout-amount {
        font-weight: 700;
        color: var(--success);
        font-size: 1rem;
      }
    }
  }

  /* 戻るセクション */
  .back-section {
    text-align: center;
    margin-top: var(--spacing-2xl);
  }

  /* ボタン */
  .btn {
    display: inline-block;
    padding: var(--spacing-md) var(--spacing-xl);
    border-radius: var(--radius-md);
    font-weight: 600;
    text-decoration: none;
    transition: all var(--transition-base);
    cursor: pointer;
    border: none;

    &.btn-primary {
      background: var(--primary-gradient);
      color: white;

      &:hover {
        transform: translateY(-2px);
        box-shadow: var(--shadow-lg);
      }
    }

    &.btn-secondary {
      background: var(--bg-secondary);
      color: var(--text-primary);
      border: 1px solid var(--border-color);

      &:hover {
        background: var(--bg-tertiary);
      }
    }
  }

  @media (max-width: 768px) {
    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .payout-grid {
      grid-template-columns: 1fr;
    }

    .horse-item {
      flex-direction: column;
      align-items: flex-start;
    }
  }
</style>
