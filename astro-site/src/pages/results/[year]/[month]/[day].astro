---
import BaseLayout from '../../../../layouts/BaseLayout.astro';

export const prerender = false;

const { year, month, day } = Astro.params;
const date = `${year}-${month}-${day}`;

// keiba-data-sharedから結果データ取得
let resultsData = null;
let archiveData = null;
let errorMessage = null;

try {
  // 結果JSONを取得
  const resultsUrl = `https://raw.githubusercontent.com/apol0510/keiba-data-shared/main/nankan/results/${year}/${month}/${date}.json`;
  const resultsResponse = await fetch(resultsUrl);

  if (!resultsResponse.ok) {
    throw new Error('結果データが見つかりません');
  }

  resultsData = await resultsResponse.json();

  // archiveResults.jsonから的中情報を取得
  const archiveUrl = `https://raw.githubusercontent.com/apol0510/keiba-intelligence/main/astro-site/src/data/archiveResults.json`;
  const archiveResponse = await fetch(archiveUrl);

  if (archiveResponse.ok) {
    const fullArchive = await archiveResponse.json();
    // 配列から該当日付を検索
    archiveData = fullArchive.find(entry => entry.date === date);
  }
} catch (error) {
  errorMessage = error.message;
}

const pageTitle = resultsData
  ? `${resultsData.date} ${resultsData.venue} 的中実績`
  : '結果データが見つかりません';

const pageDescription = archiveData
  ? `${resultsData.venue} ${archiveData.totalRaces}R開催、的中率${archiveData.hitRate}%、回収率${archiveData.returnRate}%、配当${archiveData.totalPayout.toLocaleString()}円`
  : '南関競馬の結果データ';
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <main class="archive-page">
    <div class="container">
      {errorMessage ? (
        <div class="error-section">
          <h1>⚠️ エラー</h1>
          <p>{errorMessage}</p>
          <a href="/results" class="btn-back">的中実績一覧へ戻る</a>
        </div>
      ) : resultsData && archiveData ? (
        <>
          {/* ページヘッダー */}
          <header class="page-header">
            <h1>
              <span class="date">{month}月{day}日</span>
              <span class="venue">{resultsData.venue}競馬</span>
            </h1>
          </header>

          {/* サマリー統計 */}
          <section class="summary-section">
            <div class="summary-grid">
              <div class="summary-card">
                <div class="summary-label">的中レース数</div>
                <div class="summary-value">{archiveData.hitRaces}/{archiveData.totalRaces}R</div>
              </div>
              <div class="summary-card">
                <div class="summary-label">的中率</div>
                <div class="summary-value highlight">{archiveData.hitRate}%</div>
              </div>
              <div class="summary-card">
                <div class="summary-label">回収率</div>
                <div class={`summary-value ${archiveData.returnRate >= 100 ? 'profit' : 'loss'}`}>
                  {archiveData.returnRate}%
                </div>
              </div>
              <div class="summary-card">
                <div class="summary-label">合計配当</div>
                <div class="summary-value profit">¥{archiveData.totalPayout.toLocaleString()}</div>
              </div>
            </div>
          </section>

          {/* レース結果一覧 */}
          <section class="races-section">
            <h2>📊 レース結果</h2>
            <div class="races-list">
              {resultsData.races.map((race) => {
                const archiveRace = archiveData.races.find(r => r.raceNumber === race.raceNumber);
                const isHit = archiveRace?.isHit || false;
                const payout = archiveRace?.umatan?.payout || 0;
                const bettingLines = archiveRace?.bettingLines || [];

                return (
                  <div class={`race-card ${isHit ? 'hit' : 'miss'}`}>
                    <div class="race-header">
                      <div class="race-info">
                        <span class="race-number">第{race.raceNumber}R</span>
                        <span class="race-name">{race.raceName}</span>
                      </div>
                      {isHit ? (
                        <div class="hit-badge">✅ 的中</div>
                      ) : (
                        <div class="miss-badge">❌ 不的中</div>
                      )}
                    </div>

                    <div class="race-body">
                      {/* 着順結果 */}
                      <div class="result-section">
                        <div class="result-label">着順</div>
                        <div class="result-horses">
                          <span class="result-horse rank-1">
                            <span class="rank">1着</span>
                            <span class="number">{race.results[0]?.number}番</span>
                            <span class="name">{race.results[0]?.name}</span>
                          </span>
                          <span class="result-horse rank-2">
                            <span class="rank">2着</span>
                            <span class="number">{race.results[1]?.number}番</span>
                            <span class="name">{race.results[1]?.name}</span>
                          </span>
                          <span class="result-horse rank-3">
                            <span class="rank">3着</span>
                            <span class="number">{race.results[2]?.number}番</span>
                            <span class="name">{race.results[2]?.name}</span>
                          </span>
                        </div>
                      </div>

                      {/* 購入情報 */}
                      {archiveRace && (
                        <div class="betting-section">
                          <div class="betting-info">
                            <span class="betting-type">馬単</span>
                            <span class="betting-points">{bettingLines.length}軸 約10点</span>
                          </div>
                          {isHit && payout > 0 && (
                            <div class="payout-info">
                              <span class="payout-label">配当</span>
                              <span class="payout-value">¥{(payout * 10).toLocaleString()}</span>
                            </div>
                          )}
                        </div>
                      )}

                      {/* 買い目詳細 */}
                      {archiveRace && bettingLines.length > 0 && (
                        <div class="betting-lines">
                          <div class="betting-lines-label">買い目</div>
                          <div class="betting-lines-content">
                            {bettingLines.map(line => (
                              <div class={`betting-line ${archiveRace.hitLines?.includes(line) ? 'hit-line' : ''}`}>
                                {line}
                              </div>
                            ))}
                          </div>
                        </div>
                      )}

                      {/* 馬単払戻 */}
                      {race.payouts?.umatan && (
                        <div class="official-payout">
                          <span class="official-label">馬単 ({race.payouts.umatan.combination})</span>
                          <span class="official-value">¥{race.payouts.umatan.payout?.toLocaleString()}</span>
                        </div>
                      )}
                    </div>
                  </div>
                );
              })}
            </div>
          </section>

          {/* フッターナビ */}
          <footer class="page-footer">
            <a href="/results" class="btn-back">← 的中実績一覧へ戻る</a>
          </footer>
        </>
      ) : (
        <div class="loading">読み込み中...</div>
      )}
    </div>
  </main>
</BaseLayout>

<style lang="scss">
  .archive-page {
    min-height: 100vh;
    padding: var(--spacing-2xl) 0;
    background: var(--bg-primary);
  }

  .container {
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 var(--spacing-lg);
  }

  /* エラーセクション */
  .error-section {
    text-align: center;
    padding: var(--spacing-2xl);
    background: var(--bg-secondary);
    border-radius: var(--radius-lg);

    h1 {
      font-size: 2rem;
      margin-bottom: var(--spacing-md);
      color: var(--text-primary);
    }

    p {
      color: var(--text-secondary);
      margin-bottom: var(--spacing-xl);
    }
  }

  /* ページヘッダー */
  .page-header {
    margin-bottom: var(--spacing-2xl);

    h1 {
      font-size: 2rem;
      color: var(--text-primary);
      display: flex;
      align-items: baseline;
      gap: var(--spacing-md);

      .date {
        font-weight: 800;
        background: var(--primary-gradient);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .venue {
        font-weight: 600;
        color: var(--text-secondary);
      }
    }
  }

  /* サマリーセクション */
  .summary-section {
    margin-bottom: var(--spacing-2xl);
  }

  .summary-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--spacing-lg);
  }

  .summary-card {
    background: var(--bg-secondary);
    padding: var(--spacing-xl);
    border-radius: var(--radius-lg);
    border: 1px solid var(--border-color);
    text-align: center;

    .summary-label {
      font-size: 0.875rem;
      color: var(--text-secondary);
      margin-bottom: var(--spacing-sm);
      font-weight: 500;
    }

    .summary-value {
      font-size: 2rem;
      font-weight: 800;
      color: var(--text-primary);

      &.highlight {
        color: #60a5fa;
      }

      &.profit {
        color: #10b981;
      }

      &.loss {
        color: #ef4444;
      }
    }
  }

  /* レースセクション */
  .races-section {
    h2 {
      font-size: 1.5rem;
      color: var(--text-primary);
      margin-bottom: var(--spacing-xl);
      font-weight: 700;
    }
  }

  .races-list {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-lg);
  }

  /* レースカード */
  .race-card {
    background: var(--bg-secondary);
    border-radius: var(--radius-lg);
    border: 1px solid var(--border-color);
    overflow: hidden;
    transition: all var(--transition-normal);

    &.hit {
      border-color: rgba(16, 185, 129, 0.5);
      background: linear-gradient(135deg, rgba(16, 185, 129, 0.05) 0%, var(--bg-secondary) 50%);
    }

    &:hover {
      transform: translateY(-2px);
      box-shadow: var(--shadow-lg);
    }
  }

  .race-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--spacing-lg);
    background: rgba(0, 0, 0, 0.2);
    border-bottom: 1px solid var(--border-color);
  }

  .race-info {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);

    .race-number {
      font-size: 1.125rem;
      font-weight: 700;
      color: #60a5fa;
    }

    .race-name {
      font-size: 0.875rem;
      color: var(--text-secondary);
    }
  }

  .hit-badge {
    padding: var(--spacing-xs) var(--spacing-md);
    background: rgba(16, 185, 129, 0.2);
    color: #10b981;
    border-radius: var(--radius-sm);
    font-size: 0.875rem;
    font-weight: 700;
  }

  .miss-badge {
    padding: var(--spacing-xs) var(--spacing-md);
    background: rgba(100, 116, 139, 0.2);
    color: var(--text-tertiary);
    border-radius: var(--radius-sm);
    font-size: 0.875rem;
    font-weight: 600;
  }

  .race-body {
    padding: var(--spacing-lg);
    display: flex;
    flex-direction: column;
    gap: var(--spacing-md);
  }

  /* 着順結果 */
  .result-section {
    .result-label {
      font-size: 0.75rem;
      color: var(--text-tertiary);
      margin-bottom: var(--spacing-sm);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }
  }

  .result-horses {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-xs);
  }

  .result-horse {
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);
    font-size: 0.875rem;

    .rank {
      color: var(--text-tertiary);
      min-width: 35px;
    }

    .number {
      font-weight: 700;
      color: #60a5fa;
      min-width: 40px;
    }

    .name {
      color: var(--text-primary);
    }

    &.rank-1 .rank { color: #ffd700; }
    &.rank-2 .rank { color: #c0c0c0; }
    &.rank-3 .rank { color: #cd7f32; }
  }

  /* 購入情報 */
  .betting-section {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--spacing-md);
    background: rgba(0, 0, 0, 0.2);
    border-radius: var(--radius-md);
  }

  .betting-info {
    display: flex;
    gap: var(--spacing-md);
    align-items: center;

    .betting-type {
      font-weight: 700;
      color: #60a5fa;
      font-size: 0.875rem;
    }

    .betting-points {
      font-size: 0.875rem;
      color: var(--text-secondary);
    }
  }

  .payout-info {
    display: flex;
    align-items: center;
    gap: var(--spacing-sm);

    .payout-label {
      font-size: 0.875rem;
      color: var(--text-secondary);
    }

    .payout-value {
      font-size: 1.25rem;
      font-weight: 800;
      color: #10b981;
    }
  }

  /* 買い目詳細 */
  .betting-lines {
    padding: var(--spacing-md);
    background: rgba(59, 130, 246, 0.05);
    border-radius: var(--radius-md);
    border: 1px solid rgba(59, 130, 246, 0.2);

    .betting-lines-label {
      font-size: 0.75rem;
      color: var(--text-tertiary);
      margin-bottom: var(--spacing-sm);
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .betting-lines-content {
      display: flex;
      flex-wrap: wrap;
      gap: var(--spacing-xs);
    }

    .betting-line {
      padding: var(--spacing-xs) var(--spacing-sm);
      background: rgba(0, 0, 0, 0.3);
      border-radius: var(--radius-sm);
      font-size: 0.8125rem;
      font-family: 'Courier New', monospace;
      color: var(--text-secondary);

      &.hit-line {
        background: rgba(16, 185, 129, 0.2);
        color: #10b981;
        font-weight: 700;
        border: 1px solid #10b981;
      }
    }
  }

  /* 公式払戻 */
  .official-payout {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--spacing-sm) var(--spacing-md);
    background: rgba(139, 92, 246, 0.1);
    border-radius: var(--radius-sm);
    font-size: 0.875rem;

    .official-label {
      color: var(--text-secondary);
    }

    .official-value {
      font-weight: 700;
      color: #a78bfa;
    }
  }

  /* フッター */
  .page-footer {
    margin-top: var(--spacing-2xl);
    padding-top: var(--spacing-2xl);
    border-top: 1px solid var(--border-color);
    text-align: center;
  }

  .btn-back {
    display: inline-block;
    padding: var(--spacing-md) var(--spacing-xl);
    background: var(--bg-secondary);
    color: var(--text-primary);
    text-decoration: none;
    border-radius: var(--radius-md);
    border: 1px solid var(--border-color);
    font-weight: 600;
    transition: all var(--transition-normal);

    &:hover {
      background: var(--bg-tertiary);
      transform: translateY(-2px);
    }
  }

  /* レスポンシブ */
  @media (max-width: 768px) {
    .page-header h1 {
      font-size: 1.5rem;
      flex-direction: column;
      gap: var(--spacing-xs);
    }

    .summary-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .summary-card .summary-value {
      font-size: 1.5rem;
    }

    .race-header {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--spacing-sm);
    }

    .betting-section {
      flex-direction: column;
      align-items: flex-start;
      gap: var(--spacing-sm);
    }

    .result-horses {
      font-size: 0.8125rem;
    }
  }

  .loading {
    text-align: center;
    padding: var(--spacing-2xl);
    color: var(--text-secondary);
  }
</style>
