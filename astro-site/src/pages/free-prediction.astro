---
import BaseLayout from '../layouts/BaseLayout.astro';

// 仮の予想データ（後でJSON化）
const todayDate = '2026-01-10（金）';
const venue = '大井競馬場';
const totalRaces = 12;

const samplePredictions = [
  {
    raceNumber: 10,
    raceName: '第10R 江戸前特別',
    distance: 'ダート1200m',
    grade: '3歳以上オープン',
    weather: '晴',
    track: '良',
    horses: [
      { number: 3, name: 'スピードスター', confidence: 95, prediction: '◎', odds: 2.1 },
      { number: 7, name: 'サンライズキング', confidence: 82, prediction: '○', odds: 4.5 },
      { number: 1, name: 'ダッシュウェーブ', confidence: 71, prediction: '▲', odds: 6.8 },
      { number: 5, name: 'エースファイター', confidence: 58, prediction: '△', odds: 11.2 },
      { number: 9, name: 'ラピッドフォース', confidence: 45, prediction: '☆', odds: 15.3 },
    ],
    aiComment: '◎3番スピードスターは前走好内容で今回も軸として信頼できます。○7番サンライズキングは展開が向けば届く位置にいます。',
  },
  {
    raceNumber: 11,
    raceName: '第11R 東京湾カップ',
    distance: 'ダート1600m',
    grade: '3歳以上A1',
    weather: '晴',
    track: '良',
    horses: [
      { number: 5, name: 'ゴールドラッシュ', confidence: 91, prediction: '◎', odds: 3.2 },
      { number: 2, name: 'スティールハート', confidence: 85, prediction: '○', odds: 4.1 },
      { number: 8, name: 'ファイアーボール', confidence: 74, prediction: '▲', odds: 5.9 },
      { number: 4, name: 'サンダーボルト', confidence: 62, prediction: '△', odds: 9.5 },
      { number: 1, name: 'ブレイズキング', confidence: 51, prediction: '☆', odds: 13.7 },
    ],
    aiComment: '◎5番ゴールドラッシュは近3走安定しており今回も中心。○2番スティールハートは内枠有利で差し切る可能性あり。',
  },
  {
    raceNumber: 12,
    raceName: '第12R 羽田盃',
    distance: 'ダート2000m',
    grade: '3歳以上A2',
    weather: '晴',
    track: '良',
    horses: [
      { number: 6, name: 'マイティーキング', confidence: 88, prediction: '◎', odds: 2.8 },
      { number: 3, name: 'パワーハウス', confidence: 79, prediction: '○', odds: 5.3 },
      { number: 9, name: 'ロングディスタンス', confidence: 68, prediction: '▲', odds: 7.2 },
      { number: 2, name: 'ステイヤーズ', confidence: 55, prediction: '△', odds: 10.8 },
      { number: 7, name: 'エンドランナー', confidence: 48, prediction: '☆', odds: 14.5 },
    ],
    aiComment: '◎6番マイティーキングは長距離実績豊富で今回も軸として安定。○3番パワーハウスは追い込みタイプで展開次第。',
  },
];

const stats = {
  hitRate: 78.1,
  recoveryRate: 125.3,
  totalRaces: 360,
  hits: 281,
};
---

<BaseLayout
  title="無料予想"
  description="KEIBA Intelligenceの無料予想ページ。本日の南関競馬の予想を無料で公開。AI分析による信頼度付き予想で、買い目は有料プランで提供。"
>
  <!-- ヘッダーセクション -->
  <section class="free-prediction-header gradient-bg">
    <div class="container text-center">
      <h1 class="fade-in">本日の無料予想</h1>
      <p class="section-subtitle slide-in">
        AI分析による信頼度付き予想を無料公開<br>
        買い目情報は有料プランでご提供
      </p>

      <div class="header-info">
        <div class="info-item">
          <span class="info-label">開催日</span>
          <span class="info-value">{todayDate}</span>
        </div>
        <div class="info-item">
          <span class="info-label">競馬場</span>
          <span class="info-value">{venue}</span>
        </div>
        <div class="info-item">
          <span class="info-label">レース数</span>
          <span class="info-value">{totalRaces}R</span>
        </div>
      </div>
    </div>
  </section>

  <!-- 実績バナー -->
  <section class="stats-banner">
    <div class="container">
      <div class="stats-grid">
        <div class="stat-card card">
          <div class="stat-icon">🎯</div>
          <div class="stat-value">{stats.hitRate}%</div>
          <div class="stat-label">的中率</div>
        </div>
        <div class="stat-card card">
          <div class="stat-icon">💰</div>
          <div class="stat-value">{stats.recoveryRate}%</div>
          <div class="stat-label">回収率</div>
        </div>
        <div class="stat-card card">
          <div class="stat-icon">📊</div>
          <div class="stat-value">{stats.hits}/{stats.totalRaces}</div>
          <div class="stat-label">的中数</div>
        </div>
      </div>
    </div>
  </section>

  <!-- 予想一覧 -->
  <section class="predictions">
    <div class="container">
      <div class="section-header">
        <h2 class="section-title">後半3レース予想</h2>
        <p class="section-subtitle">
          無料プランでは後半3レースの予想を公開。買い目情報を見るには<a href="/pricing">有料プラン</a>への登録が必要です。
        </p>
      </div>

      <div class="predictions-list">
        {samplePredictions.map((race) => (
          <div class="race-card card">
            <div class="race-header">
              <div class="race-title">
                <span class="race-number">第{race.raceNumber}R</span>
                <span class="race-name">{race.raceName}</span>
              </div>
              <div class="race-info">
                <span class="badge badge-info">{race.distance}</span>
                <span class="badge badge-secondary">{race.grade}</span>
                <span class="weather-info">☀️ {race.weather} / 馬場: {race.track}</span>
              </div>
            </div>

            <div class="horses-table">
              <table>
                <thead>
                  <tr>
                    <th>印</th>
                    <th>馬番</th>
                    <th>馬名</th>
                    <th>信頼度</th>
                    <th>オッズ</th>
                  </tr>
                </thead>
                <tbody>
                  {race.horses.map((horse) => (
                    <tr class={`confidence-${Math.floor(horse.confidence / 20)}`}>
                      <td class="prediction-mark">{horse.prediction}</td>
                      <td class="horse-number">{horse.number}</td>
                      <td class="horse-name">{horse.name}</td>
                      <td class="confidence">
                        <div class="confidence-bar">
                          <div
                            class="confidence-fill"
                            style={`width: ${horse.confidence}%`}
                          ></div>
                        </div>
                        <span class="confidence-value">{horse.confidence}%</span>
                      </td>
                      <td class="odds">{horse.odds}倍</td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>

            <div class="ai-comment">
              <div class="comment-header">
                <span class="comment-icon">🤖</span>
                <span class="comment-title">AI分析コメント</span>
              </div>
              <p>{race.aiComment}</p>
            </div>

            <div class="premium-cta">
              <div class="lock-icon">🔒</div>
              <div class="cta-content">
                <h3>買い目情報は有料プランで提供</h3>
                <p>馬単・三連複の買い目、穴馬情報、コース別攻略法などを有料プランでご提供しています。</p>
                <div class="cta-buttons">
                  <a href="/pricing" class="btn btn-primary">料金プランを見る</a>
                  <a href="/results" class="btn btn-secondary">的中実績を見る</a>
                </div>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- 有料プラン案内 -->
  <section class="upgrade-section gradient-bg">
    <div class="container text-center">
      <h2>有料プランで買い目をゲット</h2>
      <p class="mb-3">
        月額¥2,980から。馬単・三連複の買い目、穴馬情報、コース別攻略法を提供。<br>
        1日あたり¥99（缶コーヒー1本分）で本格的な競馬予想が利用可能。
      </p>

      <div class="upgrade-plans">
        <div class="upgrade-plan-card card">
          <div class="plan-name">ライト</div>
          <div class="plan-price">¥2,980<span>/月</span></div>
          <div class="plan-features">
            ✅ 後半3レース買い目<br>
            ✅ 馬単買い目提供
          </div>
          <a href="/pricing" class="btn btn-primary w-full">詳細を見る</a>
        </div>

        <div class="upgrade-plan-card card featured">
          <div class="badge badge-success">おすすめ</div>
          <div class="plan-name">スタンダード</div>
          <div class="plan-price">¥4,980<span>/月</span></div>
          <div class="plan-features">
            ✅ 全レース買い目<br>
            ✅ データ可視化
          </div>
          <a href="/pricing" class="btn btn-primary w-full">詳細を見る</a>
        </div>

        <div class="upgrade-plan-card card">
          <div class="plan-name">プレミアム</div>
          <div class="plan-price">¥6,980<span>/月</span></div>
          <div class="plan-features">
            ✅ 三連複買い目<br>
            ✅ 穴馬情報
          </div>
          <a href="/pricing" class="btn btn-primary w-full">詳細を見る</a>
        </div>
      </div>
    </div>
  </section>

  <!-- 注意事項 -->
  <section class="disclaimer">
    <div class="container">
      <div class="disclaimer-card card">
        <h3>⚠️ ご利用上の注意</h3>
        <ul>
          <li>本予想はAI分析に基づく参考情報であり、的中を保証するものではありません。</li>
          <li>馬券の購入は自己責任でお願いいたします。</li>
          <li>オッズは暫定値であり、レース直前に変動する可能性があります。</li>
          <li>無料予想は後半3レースのみ公開。全レース予想は有料プランでご提供します。</li>
          <li>予想データの無断転載・二次利用を禁止します。</li>
        </ul>
      </div>
    </div>
  </section>

  <style>
    /* ヘッダー */
    .free-prediction-header {
      padding: var(--spacing-2xl) 0;
      min-height: 400px;
      display: flex;
      align-items: center;
    }

    .header-info {
      display: flex;
      justify-content: center;
      gap: var(--spacing-2xl);
      margin-top: var(--spacing-xl);
    }

    .info-item {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-xs);
    }

    .info-label {
      font-size: 0.875rem;
      color: var(--text-secondary);
    }

    .info-value {
      font-size: 1.5rem;
      font-weight: 700;
      background: var(--primary-gradient);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    /* 実績バナー */
    .stats-banner {
      padding: var(--spacing-xl) 0;
      background: var(--bg-secondary);
    }

    .stats-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: var(--spacing-lg);
      max-width: 900px;
      margin: 0 auto;
    }

    .stat-card {
      text-align: center;
    }

    .stat-icon {
      font-size: 2.5rem;
      margin-bottom: var(--spacing-sm);
    }

    .stat-value {
      font-size: 2rem;
      font-weight: 700;
      background: var(--primary-gradient);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .stat-label {
      color: var(--text-secondary);
      font-size: 0.875rem;
    }

    /* 予想セクション */
    .predictions {
      padding: var(--spacing-2xl) 0;
    }

    .section-header {
      text-align: center;
      margin-bottom: var(--spacing-2xl);
    }

    .predictions-list {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-2xl);
    }

    /* レースカード */
    .race-card {
      max-width: 1000px;
      margin: 0 auto;
      width: 100%;
    }

    .race-header {
      border-bottom: 2px solid var(--border-color);
      padding-bottom: var(--spacing-md);
      margin-bottom: var(--spacing-lg);
    }

    .race-title {
      display: flex;
      align-items: center;
      gap: var(--spacing-md);
      margin-bottom: var(--spacing-sm);
    }

    .race-number {
      font-size: 1.5rem;
      font-weight: 700;
      background: var(--secondary-gradient);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .race-name {
      font-size: 1.25rem;
      font-weight: 700;
    }

    .race-info {
      display: flex;
      align-items: center;
      gap: var(--spacing-sm);
      flex-wrap: wrap;
    }

    .badge-secondary {
      background: var(--bg-tertiary);
      color: var(--text-primary);
    }

    .weather-info {
      color: var(--text-secondary);
      font-size: 0.875rem;
    }

    /* 馬一覧テーブル */
    .horses-table {
      margin-bottom: var(--spacing-lg);
      overflow-x: auto;
    }

    .horses-table table {
      width: 100%;
      border-collapse: collapse;
    }

    .horses-table th {
      padding: var(--spacing-sm);
      text-align: left;
      border-bottom: 2px solid var(--border-color);
      color: var(--text-secondary);
      font-size: 0.875rem;
    }

    .horses-table td {
      padding: var(--spacing-md) var(--spacing-sm);
      border-bottom: 1px solid var(--border-color);
    }

    .prediction-mark {
      font-size: 1.5rem;
      font-weight: 700;
      text-align: center;
    }

    .horse-number {
      font-size: 1.25rem;
      font-weight: 700;
      text-align: center;
    }

    .horse-name {
      font-weight: 600;
    }

    .confidence {
      display: flex;
      align-items: center;
      gap: var(--spacing-sm);
    }

    .confidence-bar {
      flex: 1;
      height: 8px;
      background: var(--bg-tertiary);
      border-radius: var(--radius-sm);
      overflow: hidden;
    }

    .confidence-fill {
      height: 100%;
      background: var(--primary-gradient);
      transition: width var(--transition-base);
    }

    .confidence-value {
      font-weight: 700;
      min-width: 45px;
    }

    .odds {
      font-weight: 600;
      color: var(--warning);
    }

    /* 信頼度による行の色分け */
    .confidence-4 { /* 80-100% */
      background: rgba(59, 130, 246, 0.1);
    }

    .confidence-3 { /* 60-79% */
      background: rgba(59, 130, 246, 0.05);
    }

    /* AIコメント */
    .ai-comment {
      background: var(--bg-secondary);
      border-radius: var(--radius-md);
      padding: var(--spacing-md);
      margin-bottom: var(--spacing-lg);
    }

    .comment-header {
      display: flex;
      align-items: center;
      gap: var(--spacing-sm);
      margin-bottom: var(--spacing-sm);
    }

    .comment-icon {
      font-size: 1.5rem;
    }

    .comment-title {
      font-weight: 700;
      color: var(--primary-end);
    }

    .ai-comment p {
      color: var(--text-secondary);
      font-size: 0.875rem;
      margin: 0;
    }

    /* プレミアムCTA */
    .premium-cta {
      background: var(--bg-secondary);
      border: 2px solid var(--primary-end);
      border-radius: var(--radius-lg);
      padding: var(--spacing-lg);
      display: flex;
      gap: var(--spacing-lg);
      align-items: center;
    }

    .lock-icon {
      font-size: 3rem;
      opacity: 0.5;
    }

    .cta-content {
      flex: 1;
    }

    .cta-content h3 {
      font-size: 1.25rem;
      margin-bottom: var(--spacing-sm);
    }

    .cta-content p {
      color: var(--text-secondary);
      font-size: 0.875rem;
      margin-bottom: var(--spacing-md);
    }

    .cta-buttons {
      display: flex;
      gap: var(--spacing-md);
    }

    /* アップグレードセクション */
    .upgrade-section {
      padding: var(--spacing-2xl) 0;
    }

    .upgrade-plans {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: var(--spacing-lg);
      max-width: 900px;
      margin: 0 auto;
    }

    .upgrade-plan-card {
      text-align: center;
    }

    .upgrade-plan-card.featured {
      border: 2px solid var(--primary-end);
      transform: scale(1.05);
    }

    .upgrade-plan-card .badge {
      position: absolute;
      top: var(--spacing-md);
      right: var(--spacing-md);
    }

    .plan-name {
      font-size: 1.5rem;
      font-weight: 700;
      margin-bottom: var(--spacing-sm);
    }

    .plan-price {
      font-size: 2rem;
      font-weight: 700;
      background: var(--primary-gradient);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: var(--spacing-md);
    }

    .plan-price span {
      font-size: 1rem;
      color: var(--text-secondary);
    }

    .plan-features {
      color: var(--text-secondary);
      font-size: 0.875rem;
      margin-bottom: var(--spacing-md);
      line-height: 1.8;
    }

    /* 注意事項 */
    .disclaimer {
      padding: var(--spacing-2xl) 0;
    }

    .disclaimer-card {
      max-width: 1000px;
      margin: 0 auto;
      background: rgba(245, 158, 11, 0.1);
      border: 1px solid var(--warning);
    }

    .disclaimer-card h3 {
      color: var(--warning);
      margin-bottom: var(--spacing-md);
    }

    .disclaimer-card ul {
      list-style: none;
      padding: 0;
    }

    .disclaimer-card li {
      padding: var(--spacing-xs) 0;
      color: var(--text-secondary);
      font-size: 0.875rem;
    }

    .disclaimer-card li:before {
      content: "• ";
      color: var(--warning);
      font-weight: 700;
      margin-right: var(--spacing-xs);
    }

    /* レスポンシブ */
    @media (max-width: 1024px) {
      .upgrade-plans {
        grid-template-columns: 1fr;
      }

      .upgrade-plan-card.featured {
        transform: scale(1);
      }

      .header-info {
        flex-direction: column;
        gap: var(--spacing-md);
      }
    }

    @media (max-width: 768px) {
      .stats-grid {
        grid-template-columns: 1fr;
      }

      .premium-cta {
        flex-direction: column;
        text-align: center;
      }

      .cta-buttons {
        flex-direction: column;
      }

      .horses-table {
        font-size: 0.875rem;
      }

      .confidence {
        flex-direction: column;
        align-items: flex-start;
      }

      .confidence-bar {
        width: 100%;
      }
    }
  </style>
</BaseLayout>
