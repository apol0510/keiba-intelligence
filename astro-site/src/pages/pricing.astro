---
import BaseLayout from '../layouts/BaseLayout.astro';

export const prerender = true;

const recommendedPlans = [
  {
    id: 'lifetime',
    name: '買い切りプラン',
    price: 88000,
    period: '',
    description: '南関4場＋中央競馬（JRA全10場）永久アクセス',
    features: [
      { text: '南関4場全レース予想', included: true },
      { text: 'JRA全10場全レース予想', included: true },
      { text: '馬単買い目提供', included: true },
      { text: '的中実績閲覧', included: true },
      { text: 'データ可視化', included: true },
      { text: 'Feature Importance Analysis', included: true },
      { text: '永久アクセス権', included: true, highlight: true },
    ],
    cta: '買い切りプランを始める',
    ctaLink: '/login',
    highlighted: true,
    badge: '一番お得',
    lifetimeNote: '3年利用で月額換算2,444円（年払いの半額以下）',
  },
  {
    id: 'yearly',
    name: '年払いプラン',
    price: 66000,
    originalPrice: 144000, // 月払い12,000円×12ヶ月
    period: '/年',
    description: '南関4場＋中央競馬（JRA全10場）',
    features: [
      { text: '南関4場全レース予想', included: true },
      { text: 'JRA全10場全レース予想', included: true },
      { text: '馬単買い目提供', included: true },
      { text: '的中実績閲覧', included: true },
      { text: 'データ可視化', included: true },
      { text: 'Feature Importance Analysis', included: true },
    ],
    cta: '年払いプランを始める',
    ctaLink: '/login',
    highlighted: true,
    badge: 'おすすめ',
    monthlyEquivalent: 5500,
    dailyPrice: 181,
    savings: 78000, // 144,000 - 66,000
  },
];

const monthlyPlans = [
  {
    id: 'monthly-nankan',
    name: '南関のみ（月払い）',
    price: 12000,
    period: '/月',
    description: '南関4場のみ',
    features: [
      { text: '南関4場全レース予想', included: true },
      { text: '馬単買い目提供', included: true },
      { text: '的中実績閲覧', included: true },
      { text: 'データ可視化', included: true },
      { text: 'JRA予想', included: false },
    ],
    cta: '月払いで始める',
    ctaLink: '/login',
    highlighted: false,
    warning: '年払いより2.2倍割高',
    dailyPrice: 400,
  },
  {
    id: 'monthly-jra',
    name: '中央のみ（月払い）',
    price: 12000,
    period: '/月',
    description: 'JRA全10場のみ',
    features: [
      { text: 'JRA全10場全レース予想', included: true },
      { text: '馬単買い目提供', included: true },
      { text: '的中実績閲覧', included: true },
      { text: 'データ可視化', included: true },
      { text: '南関予想', included: false },
    ],
    cta: '月払いで始める',
    ctaLink: '/login',
    highlighted: false,
    warning: '年払いより2.2倍割高',
    dailyPrice: 400,
  },
];
---

<BaseLayout
  title="料金プラン"
  description="KEIBA Intelligenceの料金プラン。買い切り¥88,000（永久アクセス、3年利用で月額換算¥2,444）または年払い¥66,000。南関4場＋中央競馬（JRA全10場）全レース対応。"
>
  <!-- ヘッダーセクション -->
  <section class="pricing-header gradient-bg">
    <div class="container text-center">
      <h1 class="fade-in">料金プラン</h1>
      <p class="section-subtitle slide-in">
        買い切り¥88,000（永久アクセス）または年払い¥66,000。<br>
        南関4場＋中央競馬（JRA全10場）全レース対応。
      </p>
    </div>
  </section>

  <!-- 365日競馬バナー -->
  <section class="daily-racing-banner">
    <div class="container">
      <div class="banner-card">
        <div class="banner-icon">🏇</div>
        <h2 class="banner-title">365日、毎日競馬ができる</h2>
        <p class="banner-subtitle">平日も週末も、年中無休で競馬予想を提供</p>

        <div class="racing-schedule">
          <div class="schedule-item">
            <div class="schedule-days">月〜金</div>
            <div class="schedule-icon">🌙</div>
            <div class="schedule-venue">南関競馬</div>
            <div class="schedule-desc">大井・川崎・船橋・浦和</div>
          </div>
          <div class="schedule-divider">+</div>
          <div class="schedule-item">
            <div class="schedule-days">土・日</div>
            <div class="schedule-icon">☀️</div>
            <div class="schedule-venue">中央競馬</div>
            <div class="schedule-desc">JRA全10場</div>
          </div>
        </div>

        <div class="banner-highlight">
          <div class="lifetime-access-huge">永久アクセス</div>
          <div class="banner-highlight-text">
            買い切りプラン <span class="highlight-price">¥88,000</span> なら、<br>
            この環境が永久に手に入る
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- おすすめプラン（年払い・買い切り） -->
  <section class="pricing-plans">
    <div class="container">
      <h2 class="section-title text-center">おすすめプラン</h2>
      <p class="text-center mb-3" style="color: var(--text-secondary);">
        南関4場＋中央競馬（JRA全10場）セット
      </p>

      <div class="grid grid-2" style="max-width: 900px; margin: 0 auto;">
        {recommendedPlans.map((plan) => (
          <div class={`pricing-card card ${plan.highlighted ? 'featured' : ''} ${plan.id === 'lifetime' ? 'lifetime-special' : ''}`}>
            {plan.badge && (
              <div class={`badge ${plan.id === 'lifetime' ? 'badge-lifetime' : 'badge-success'}`}>{plan.badge}</div>
            )}

            <div class="plan-header">
              <div class="plan-name">{plan.name}</div>
              <div class="plan-price">
                <span class="price">¥{plan.price.toLocaleString()}</span>
                <span class="period">{plan.period}</span>
              </div>
              {plan.monthlyEquivalent && (
                <div class="monthly-equivalent">
                  月額換算 ¥{plan.monthlyEquivalent.toLocaleString()}/月
                </div>
              )}
              {plan.dailyPrice && (
                <div class="daily-price">1日あたり¥{plan.dailyPrice}</div>
              )}
              {plan.savings && (
                <div class="savings-badge">
                  年間¥{plan.savings.toLocaleString()}お得
                </div>
              )}
              {plan.lifetimeNote && (
                <div class="lifetime-note-big">{plan.lifetimeNote}</div>
              )}
              <div class="plan-description">{plan.description}</div>
            </div>

            <ul class="plan-features">
              {plan.features.map((feature) => (
                <li class={`${feature.included ? 'included' : 'excluded'} ${feature.highlight ? 'highlight' : ''}`}>
                  {feature.included ? '✅' : '❌'} {feature.text}
                </li>
              ))}
            </ul>

            {plan.id === 'lifetime' && (
              <>
                <div class="lifetime-calculation">
                  <div class="calc-title">💰 長期利用でさらにお得</div>
                  <div class="calc-grid">
                    <div class="calc-item">
                      <div class="calc-period">3年利用</div>
                      <div class="calc-value">¥2,444/月</div>
                    </div>
                    <div class="calc-item">
                      <div class="calc-period">5年利用</div>
                      <div class="calc-value">¥1,467/月</div>
                    </div>
                    <div class="calc-item">
                      <div class="calc-period">10年利用</div>
                      <div class="calc-value">¥733/月</div>
                    </div>
                  </div>
                </div>

                <div class="lifetime-future">
                  <div class="future-title">🚀 50年後も、追加料金なし</div>

                  <ul class="future-features">
                    <li>✅ AIは自動で動き続ける（2076年も使える）</li>
                    <li>✅ 毎日進化し、精度が上がる</li>
                    <li>✅ ¥88,000以外、一生払わない</li>
                  </ul>

                  <div class="future-comparison">
                    <div class="comparison-label">月払いなら50年で</div>
                    <div class="comparison-monthly">¥7,200,000</div>
                    <div class="comparison-arrow">↓</div>
                    <div class="comparison-label">買い切りなら</div>
                    <div class="comparison-lifetime">¥88,000のみ</div>
                  </div>

                  <div class="future-tagline">
                    未来を買う、今だけ¥88,000
                  </div>
                </div>
              </>
            )}

            <a href={plan.ctaLink} class={`btn ${plan.id === 'lifetime' ? 'btn-lifetime' : 'btn-primary'} w-full`}>
              {plan.cta}
            </a>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- 月払いプラン（割高・非推奨） -->
  <section class="monthly-plans">
    <div class="container">
      <h2 class="section-title text-center">月払いプラン（割高・非推奨）</h2>
      <p class="text-center mb-3" style="color: #ef4444; font-weight: 700;">
        ⚠️ 年払いより2.2倍割高です。年払いプランを強くおすすめします。
      </p>

      <div class="grid grid-2" style="max-width: 900px; margin: 0 auto;">
        {monthlyPlans.map((plan) => (
          <div class="pricing-card card monthly-warning">
            {plan.warning && (
              <div class="badge badge-warning">{plan.warning}</div>
            )}

            <div class="plan-header">
              <div class="plan-name">{plan.name}</div>
              <div class="plan-price">
                <span class="price">¥{plan.price.toLocaleString()}</span>
                <span class="period">{plan.period}</span>
              </div>
              {plan.dailyPrice && (
                <div class="daily-price warning-text">1日あたり¥{plan.dailyPrice}</div>
              )}
              <div class="plan-description">{plan.description}</div>
            </div>

            <ul class="plan-features">
              {plan.features.map((feature) => (
                <li class={feature.included ? 'included' : 'excluded'}>
                  {feature.included ? '✅' : '❌'} {feature.text}
                </li>
              ))}
            </ul>

            <a href={plan.ctaLink} class="btn btn-secondary w-full">
              {plan.cta}
            </a>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- 価格比較表 -->
  <section class="price-comparison">
    <div class="container">
      <h2 class="section-title text-center">価格比較</h2>

      <div class="comparison-table-wrapper">
        <table class="comparison-table">
          <thead>
            <tr>
              <th>プラン</th>
              <th>価格</th>
              <th>月額換算</th>
              <th>1日あたり</th>
              <th>お得度</th>
            </tr>
          </thead>
          <tbody>
            <tr class="highlight-row">
              <td><strong>買い切り（南関＋中央）</strong></td>
              <td><strong>¥88,000（永久）</strong></td>
              <td><strong>¥2,444/月</strong><br><small>（3年利用時）</small></td>
              <td><strong>¥81/日</strong><br><small>（3年利用時）</small></td>
              <td><span class="badge badge-success">3年で¥256,000お得</span></td>
            </tr>
            <tr class="highlight-row">
              <td><strong>年払い（南関＋中央）</strong></td>
              <td><strong>¥66,000/年</strong></td>
              <td><strong>¥5,500/月</strong></td>
              <td><strong>¥181/日</strong></td>
              <td><span class="badge badge-success">年間¥78,000お得</span></td>
            </tr>
            <tr class="warning-row">
              <td>月払い（南関のみ）</td>
              <td>¥12,000/月</td>
              <td>¥12,000/月</td>
              <td>¥400/日</td>
              <td><span class="badge badge-warning">年払いより2.2倍割高</span></td>
            </tr>
            <tr class="warning-row">
              <td>月払い（中央のみ）</td>
              <td>¥12,000/月</td>
              <td>¥12,000/月</td>
              <td>¥400/日</td>
              <td><span class="badge badge-warning">年払いより2.2倍割高</span></td>
            </tr>
          </tbody>
        </table>
      </div>

      <div class="comparison-note">
        <p>💎 <strong>買い切りプランなら：</strong></p>
        <ul>
          <li>3年利用で月額換算¥2,444（年払いの半額以下）</li>
          <li>5年利用なら月額換算¥1,467（月払いの1/8）</li>
          <li>永久アクセス権で将来の値上げリスクなし</li>
          <li>長期利用すればするほどお得</li>
        </ul>
        <p>💡 <strong>年払いプランなら：</strong></p>
        <ul>
          <li>月額換算¥5,500で南関＋中央の両方が利用可能</li>
          <li>1日あたりわずか¥181（缶コーヒー1本分以下）</li>
          <li>月払いの半額以下でお得</li>
        </ul>
      </div>
    </div>
  </section>

  <!-- プラン機能比較表 -->
  <section class="plan-comparison">
    <div class="container">
      <h2 class="section-title text-center">プラン機能比較表</h2>

      <div class="comparison-table-wrapper">
        <table class="comparison-table">
          <thead>
            <tr>
              <th>機能</th>
              <th class="highlighted">買い切り</th>
              <th class="highlighted">年払い</th>
              <th>月払い南関</th>
              <th>月払い中央</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>南関4場全レース予想</td>
              <td>✅</td>
              <td>✅</td>
              <td>✅</td>
              <td>❌</td>
            </tr>
            <tr>
              <td>JRA全10場全レース予想</td>
              <td>✅</td>
              <td>✅</td>
              <td>❌</td>
              <td>✅</td>
            </tr>
            <tr>
              <td>馬単買い目提供</td>
              <td>✅</td>
              <td>✅</td>
              <td>✅</td>
              <td>✅</td>
            </tr>
            <tr>
              <td>的中実績閲覧</td>
              <td>✅</td>
              <td>✅</td>
              <td>✅</td>
              <td>✅</td>
            </tr>
            <tr>
              <td>データ可視化</td>
              <td>✅</td>
              <td>✅</td>
              <td>✅</td>
              <td>✅</td>
            </tr>
            <tr>
              <td>Feature Importance Analysis</td>
              <td>✅</td>
              <td>✅</td>
              <td>✅</td>
              <td>✅</td>
            </tr>
            <tr>
              <td>永久アクセス</td>
              <td>✅</td>
              <td>❌</td>
              <td>❌</td>
              <td>❌</td>
            </tr>
            <tr class="highlight-row">
              <td><strong>月額換算</strong></td>
              <td><strong>¥2,444</strong><br><small>(3年利用時)</small></td>
              <td><strong>¥5,500</strong></td>
              <td>¥12,000</td>
              <td>¥12,000</td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="faq">
    <div class="container">
      <h2 class="section-title text-center">よくある質問</h2>

      <div class="faq-grid">
        <div class="faq-item card">
          <h3>Q. おすすめのプランはどれですか？</h3>
          <p>
            <strong>買い切りプラン（¥88,000）</strong>を最もおすすめします。3年利用で月額換算¥2,444（年払いの半額以下）、5年なら¥1,467と、長期利用で圧倒的にお得になります。
          </p>
          <p>
            1年程度の利用を予定されている方は<strong>年払いプラン（¥66,000）</strong>もおすすめです。月額換算¥5,500（1日¥181）で月払いより54%お得です。
          </p>
        </div>

        <div class="faq-item card">
          <h3>Q. 月払いと年払いの差は？</h3>
          <p>
            月払いは<strong>¥12,000/月（年間¥144,000）</strong>、年払いは<strong>¥66,000/年</strong>です。
            年払いなら<strong>年間¥78,000（54%）お得</strong>になります。
          </p>
        </div>

        <div class="faq-item card">
          <h3>Q. 買い切りプランの「永久アクセス」とは？</h3>
          <p>
            買い切りプラン（¥88,000）は<strong>一度の購入で永久に利用可能</strong>です。追加料金は一切発生しません。
            3年利用で月額換算¥2,444、5年なら¥1,467と、利用期間が長いほどお得になります。
          </p>
        </div>

        <div class="faq-item card highlight-faq">
          <h3>Q. 50年後も本当に使えるの？AIは進化する？</h3>
          <p>
            <strong>はい、50年後も追加料金なしで使えます。</strong>
            AIシステムは自動で動き続け、毎日進化を重ねます。2076年のAIがどれだけ進化しているか、誰にもわかりません。
          </p>
          <p>
            でも、ひとつ確実なこと。<strong>あなたが支払うのは2026年の¥88,000だけ</strong>です。
            月払いなら50年で¥720万。買い切りなら¥88,000のみ。それが買い切りの力です。
          </p>
        </div>

        <div class="faq-item card">
          <h3>Q. 南関のみ・中央のみのプランはありますか？</h3>
          <p>
            月払い（¥12,000/月）でのみ南関のみ・中央のみのプランをご用意していますが、<strong>年払いプランの方が圧倒的にお得</strong>です。
            南関＋中央セットの年払い（¥66,000）なら、月払いの半額以下で両方利用できます。
          </p>
        </div>

        <div class="faq-item card">
          <h3>Q. 支払い方法は？</h3>
          <p>
            現在、<strong>銀行振り込みのみ</strong>対応しています。お申し込み後、振込先情報をメールでお送りします。
            入金確認後、即座にアクセス権を付与いたします。
          </p>
        </div>

        <div class="faq-item card">
          <h3>Q. 途中解約・返金はできますか？</h3>
          <p>
            年払いプラン・買い切りプランともに、<strong>購入後の返金は原則として承っておりません</strong>。
            無料予想ページで予想の精度を事前にご確認の上、お申し込みください。
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="cta gradient-bg">
    <div class="container text-center">
      <h2>今すぐ始めて、勝ち馬を見つけよう</h2>
      <p class="mb-3">買い切り¥88,000（永久アクセス）または年払い¥66,000</p>
      <div class="cta-buttons">
        <a href="/login" class="btn btn-primary btn-lg">買い切りプランを始める</a>
        <a href="/login" class="btn btn-secondary btn-lg">年払いプランを始める</a>
      </div>
    </div>
  </section>

  <style>
    /* ヘッダー */
    .pricing-header {
      padding: var(--spacing-2xl) 0;
      text-align: center;
    }

    .pricing-header h1 {
      font-size: 3rem;
      margin-bottom: var(--spacing-lg);
      font-weight: 800;
      background: linear-gradient(135deg, #e0f2fe 0%, #60a5fa 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .section-subtitle {
      font-size: 1.25rem;
      color: var(--text-secondary);
      line-height: 1.6;
    }

    /* 365日競馬バナー */
    .daily-racing-banner {
      padding: var(--spacing-2xl) 0;
      background: linear-gradient(135deg,
        rgba(16, 185, 129, 0.1) 0%,
        rgba(59, 130, 246, 0.1) 100%);
    }

    .banner-card {
      background: var(--bg-tertiary);
      padding: var(--spacing-2xl);
      border-radius: var(--radius-lg);
      border: 2px solid rgba(16, 185, 129, 0.3);
      text-align: center;
    }

    .banner-icon {
      font-size: 4rem;
      margin-bottom: var(--spacing-md);
      animation: bounce 2s infinite;
    }

    @keyframes bounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }

    .banner-title {
      font-size: 2.5rem;
      font-weight: 900;
      background: linear-gradient(135deg, #10b981 0%, #3b82f6 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: var(--spacing-md);
    }

    .banner-subtitle {
      font-size: 1.25rem;
      color: var(--text-secondary);
      margin-bottom: var(--spacing-xl);
    }

    .racing-schedule {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: var(--spacing-xl);
      margin-bottom: var(--spacing-xl);
      flex-wrap: wrap;
    }

    .schedule-item {
      background: var(--bg-secondary);
      padding: var(--spacing-lg);
      border-radius: var(--radius-md);
      border: 2px solid rgba(59, 130, 246, 0.3);
      min-width: 200px;
    }

    .schedule-days {
      font-size: 1.5rem;
      font-weight: 700;
      color: #60a5fa;
      margin-bottom: var(--spacing-sm);
    }

    .schedule-icon {
      font-size: 2rem;
      margin-bottom: var(--spacing-sm);
    }

    .schedule-venue {
      font-size: 1.25rem;
      font-weight: 700;
      color: var(--text-primary);
      margin-bottom: var(--spacing-xs);
    }

    .schedule-desc {
      font-size: 0.875rem;
      color: var(--text-secondary);
    }

    .schedule-divider {
      font-size: 2rem;
      font-weight: 700;
      color: #10b981;
    }

    .banner-highlight {
      background: linear-gradient(135deg,
        rgba(139, 92, 246, 0.2) 0%,
        rgba(167, 139, 250, 0.2) 100%);
      padding: var(--spacing-xl);
      border-radius: var(--radius-md);
      border: 2px solid rgba(139, 92, 246, 0.5);
      text-align: center;
    }

    .lifetime-access-huge {
      font-size: 3.5rem;
      font-weight: 900;
      background: linear-gradient(135deg, #8b5cf6 0%, #a78bfa 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: var(--spacing-md);
      letter-spacing: 2px;
      text-shadow: 0 0 30px rgba(139, 92, 246, 0.5);
      animation: glow 2s ease-in-out infinite;
    }

    @keyframes glow {
      0%, 100% { filter: brightness(1); }
      50% { filter: brightness(1.3); }
    }

    .banner-highlight-text {
      font-size: 1.25rem;
      color: #a78bfa;
      font-weight: 700;
    }

    .highlight-price {
      font-size: 1.5rem;
      font-weight: 900;
      color: #c4b5fd;
    }

    /* プランセクション */
    .pricing-plans, .monthly-plans, .price-comparison, .plan-comparison, .faq {
      padding: var(--spacing-2xl) 0;
    }

    .monthly-plans {
      background: var(--bg-secondary);
    }

    .section-title {
      font-size: 2.5rem;
      margin-bottom: var(--spacing-xl);
      text-align: center;
    }

    /* プランカード */
    .pricing-card {
      position: relative;
      text-align: center;
      padding: var(--spacing-xl);
      transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .pricing-card:hover {
      transform: translateY(-5px);
    }

    .pricing-card.featured {
      border: 2px solid var(--primary-end);
      box-shadow: 0 0 30px rgba(59, 130, 246, 0.3);
    }

    .pricing-card.lifetime-special {
      border: 3px solid #a78bfa;
      box-shadow: 0 0 40px rgba(167, 139, 250, 0.5);
      background: linear-gradient(135deg,
        rgba(139, 92, 246, 0.05) 0%,
        rgba(167, 139, 250, 0.05) 100%);
      transform: scale(1.05);
    }

    .pricing-card.lifetime-special:hover {
      transform: scale(1.08);
    }

    .pricing-card.monthly-warning {
      border: 2px solid rgba(239, 68, 68, 0.3);
      opacity: 0.85;
    }

    .pricing-card .badge {
      position: absolute;
      top: var(--spacing-md);
      right: var(--spacing-md);
      font-size: 0.75rem;
      padding: 6px 12px;
    }

    .badge-warning {
      background: rgba(239, 68, 68, 0.2);
      border: 1px solid rgba(239, 68, 68, 0.5);
      color: #ef4444;
    }

    .badge-lifetime {
      background: linear-gradient(135deg, #8b5cf6 0%, #a78bfa 100%);
      color: white;
      font-size: 0.875rem;
      padding: 8px 16px;
      font-weight: 900;
      letter-spacing: 0.5px;
      animation: pulse 2s infinite;
    }

    @keyframes pulse {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.05); }
    }

    .plan-header {
      margin-bottom: var(--spacing-lg);
    }

    .plan-name {
      font-size: 1.75rem;
      font-weight: 700;
      margin-bottom: var(--spacing-md);
      color: var(--text-primary);
    }

    .plan-price {
      margin-bottom: var(--spacing-sm);
    }

    .price {
      font-size: 3rem;
      font-weight: 700;
      background: var(--primary-gradient);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .period {
      font-size: 1.125rem;
      color: var(--text-secondary);
      margin-left: var(--spacing-xs);
    }

    .monthly-equivalent {
      font-size: 1rem;
      color: #10b981;
      font-weight: 700;
      margin-bottom: var(--spacing-xs);
    }

    .daily-price {
      font-size: 0.875rem;
      color: var(--text-secondary);
      margin-bottom: var(--spacing-xs);
    }

    .daily-price.warning-text {
      color: #ef4444;
      font-weight: 700;
    }

    .savings-badge {
      display: inline-block;
      padding: 6px 12px;
      background: rgba(16, 185, 129, 0.2);
      border: 1px solid rgba(16, 185, 129, 0.5);
      border-radius: 12px;
      font-size: 0.875rem;
      font-weight: 700;
      color: #10b981;
      margin-bottom: var(--spacing-sm);
    }

    .lifetime-note {
      font-size: 0.875rem;
      color: #a78bfa;
      font-weight: 700;
      margin-bottom: var(--spacing-sm);
    }

    .lifetime-note-big {
      font-size: 1rem;
      color: #a78bfa;
      font-weight: 700;
      margin-bottom: var(--spacing-sm);
      background: rgba(167, 139, 250, 0.1);
      padding: var(--spacing-sm);
      border-radius: var(--radius-sm);
    }

    .lifetime-calculation {
      background: var(--bg-secondary);
      padding: var(--spacing-lg);
      border-radius: var(--radius-md);
      margin-bottom: var(--spacing-lg);
      border: 2px solid rgba(167, 139, 250, 0.3);
    }

    .calc-title {
      font-size: 1rem;
      font-weight: 700;
      color: #a78bfa;
      margin-bottom: var(--spacing-md);
      text-align: center;
    }

    .calc-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: var(--spacing-md);
    }

    .calc-item {
      text-align: center;
      padding: var(--spacing-sm);
      background: rgba(167, 139, 250, 0.1);
      border-radius: var(--radius-sm);
    }

    .calc-period {
      font-size: 0.875rem;
      color: var(--text-secondary);
      margin-bottom: var(--spacing-xs);
    }

    .calc-value {
      font-size: 1.25rem;
      font-weight: 900;
      color: #a78bfa;
      font-family: 'Courier New', monospace;
    }

    .lifetime-future {
      background: linear-gradient(135deg,
        rgba(16, 185, 129, 0.1) 0%,
        rgba(59, 130, 246, 0.1) 100%);
      padding: var(--spacing-xl);
      border-radius: var(--radius-md);
      margin-bottom: var(--spacing-lg);
      border: 2px solid rgba(16, 185, 129, 0.5);
    }

    .future-title {
      font-size: 1.5rem;
      font-weight: 900;
      background: linear-gradient(135deg, #10b981 0%, #3b82f6 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: var(--spacing-md);
      text-align: center;
    }

    .future-features {
      list-style: none;
      margin-bottom: var(--spacing-lg);
      text-align: left;
    }

    .future-features li {
      padding: var(--spacing-sm) 0;
      color: var(--text-primary);
      font-size: 1rem;
      font-weight: 600;
      line-height: 1.6;
    }

    .future-comparison {
      background: var(--bg-secondary);
      padding: var(--spacing-lg);
      border-radius: var(--radius-sm);
      text-align: center;
      margin-bottom: var(--spacing-md);
    }

    .comparison-label {
      font-size: 0.875rem;
      color: var(--text-secondary);
      margin-bottom: var(--spacing-xs);
    }

    .comparison-monthly {
      font-size: 1.75rem;
      font-weight: 900;
      color: #ef4444;
      font-family: 'Courier New', monospace;
      text-decoration: line-through;
      margin-bottom: var(--spacing-sm);
    }

    .comparison-arrow {
      font-size: 1.5rem;
      color: #10b981;
      margin-bottom: var(--spacing-sm);
    }

    .comparison-lifetime {
      font-size: 2rem;
      font-weight: 900;
      color: #10b981;
      font-family: 'Courier New', monospace;
    }

    .future-tagline {
      font-size: 1.125rem;
      font-weight: 700;
      color: #60a5fa;
      text-align: center;
      padding: var(--spacing-md);
      background: rgba(59, 130, 246, 0.1);
      border-radius: var(--radius-sm);
    }

    .plan-description {
      color: var(--text-secondary);
      margin-bottom: var(--spacing-lg);
      font-size: 0.9rem;
    }

    /* プラン機能リスト */
    .plan-features {
      list-style: none;
      text-align: left;
      margin-bottom: var(--spacing-lg);
    }

    .plan-features li {
      padding: var(--spacing-sm) 0;
      color: var(--text-secondary);
      border-bottom: 1px solid var(--border-color);
    }

    .plan-features li:last-child {
      border-bottom: none;
    }

    .plan-features li.included {
      color: var(--text-primary);
    }

    .plan-features li.excluded {
      opacity: 0.5;
    }

    .plan-features li.highlight {
      color: #a78bfa;
      font-weight: 900;
      font-size: 1.125rem;
      background: rgba(167, 139, 250, 0.1);
      padding: var(--spacing-md);
      border-radius: var(--radius-sm);
      border: 2px solid rgba(167, 139, 250, 0.5);
      animation: pulse-slow 3s infinite;
    }

    @keyframes pulse-slow {
      0%, 100% { transform: scale(1); opacity: 1; }
      50% { transform: scale(1.02); opacity: 0.95; }
    }

    /* ボタン */
    .w-full {
      width: 100%;
    }

    .btn-lg {
      padding: var(--spacing-md) var(--spacing-xl);
      font-size: 1.125rem;
    }

    .btn-lifetime {
      background: linear-gradient(135deg, #8b5cf6 0%, #a78bfa 100%);
      color: white;
      font-weight: 900;
      font-size: 1.125rem;
      padding: var(--spacing-md) var(--spacing-xl);
      border: none;
      box-shadow: 0 4px 20px rgba(139, 92, 246, 0.5);
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .btn-lifetime:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 30px rgba(139, 92, 246, 0.7);
    }

    /* 価格比較表 */
    .comparison-table-wrapper {
      overflow-x: auto;
      margin-bottom: var(--spacing-xl);
    }

    .comparison-table {
      width: 100%;
      border-collapse: collapse;
      background: var(--bg-tertiary);
      border-radius: var(--radius-lg);
      overflow: hidden;
    }

    .comparison-table th,
    .comparison-table td {
      padding: var(--spacing-md);
      text-align: center;
      border-bottom: 1px solid var(--border-color);
    }

    .comparison-table th {
      background: var(--bg-secondary);
      font-weight: 700;
      color: var(--text-primary);
    }

    .comparison-table th.highlighted {
      background: linear-gradient(135deg, rgba(59, 130, 246, 0.2), rgba(139, 92, 246, 0.2));
      color: #60a5fa;
    }

    .comparison-table tbody tr:hover {
      background: rgba(59, 130, 246, 0.05);
    }

    .comparison-table .highlight-row {
      background: rgba(16, 185, 129, 0.1);
      font-weight: 700;
    }

    .comparison-table .warning-row {
      background: rgba(239, 68, 68, 0.05);
      opacity: 0.7;
    }

    .comparison-note {
      background: var(--bg-secondary);
      padding: var(--spacing-xl);
      border-radius: var(--radius-lg);
      border: 2px solid rgba(16, 185, 129, 0.3);
    }

    .comparison-note p {
      color: var(--text-primary);
      font-size: 1rem;
      margin-bottom: var(--spacing-md);
    }

    .comparison-note ul {
      list-style: none;
      margin-bottom: var(--spacing-md);
    }

    .comparison-note li {
      padding: var(--spacing-xs) 0;
      color: var(--text-secondary);
    }

    .comparison-note li:before {
      content: '→';
      margin-right: var(--spacing-xs);
      color: #10b981;
    }

    /* FAQ */
    .faq-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: var(--spacing-lg);
    }

    .faq-item h3 {
      font-size: 1.125rem;
      color: var(--text-primary);
      margin-bottom: var(--spacing-md);
    }

    .faq-item p {
      color: var(--text-secondary);
      line-height: 1.6;
      margin-bottom: var(--spacing-sm);
    }

    .faq-item.highlight-faq {
      border: 2px solid rgba(16, 185, 129, 0.5);
      background: linear-gradient(135deg,
        rgba(16, 185, 129, 0.05) 0%,
        rgba(59, 130, 246, 0.05) 100%);
    }

    .faq-item.highlight-faq h3 {
      background: linear-gradient(135deg, #10b981 0%, #3b82f6 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    /* CTA */
    .cta {
      padding: var(--spacing-2xl) 0;
      margin-top: var(--spacing-2xl);
    }

    .cta h2 {
      font-size: 2rem;
      color: var(--text-primary);
      margin-bottom: var(--spacing-md);
    }

    .cta p {
      font-size: 1.125rem;
      color: var(--text-secondary);
    }

    .cta-buttons {
      display: flex;
      gap: var(--spacing-md);
      justify-content: center;
      flex-wrap: wrap;
    }

    /* レスポンシブ */
    @media (max-width: 1024px) {
      .faq-grid {
        grid-template-columns: 1fr;
      }

      .racing-schedule {
        flex-direction: column;
      }

      .calc-grid {
        grid-template-columns: 1fr;
      }

      .future-title {
        font-size: 1.25rem;
      }

      .comparison-monthly {
        font-size: 1.5rem;
      }

      .comparison-lifetime {
        font-size: 1.75rem;
      }
    }

    @media (max-width: 768px) {
      .pricing-header h1 {
        font-size: 2rem;
      }

      .section-title {
        font-size: 1.75rem;
      }

      .banner-title {
        font-size: 1.75rem;
      }

      .lifetime-access-huge {
        font-size: 2.5rem;
      }

      .banner-highlight-text {
        font-size: 1rem;
      }

      .highlight-price {
        font-size: 1.25rem;
      }

      .grid-2 {
        grid-template-columns: 1fr;
      }

      .pricing-card.lifetime-special {
        transform: scale(1);
      }

      .pricing-card.lifetime-special:hover {
        transform: scale(1.02);
      }

      .cta-buttons {
        flex-direction: column;
      }

      .comparison-table {
        font-size: 0.875rem;
      }

      .comparison-table th,
      .comparison-table td {
        padding: var(--spacing-sm);
      }
    }
  </style>
</BaseLayout>
