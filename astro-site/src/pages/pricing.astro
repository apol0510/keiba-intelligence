---
import BaseLayout from '../layouts/BaseLayout.astro';

export const prerender = true;

const plans = [
  {
    id: 'free',
    name: 'フリー',
    price: 0,
    yearlyPrice: 0,
    period: '無料',
    description: '予想閲覧のみ（買い目なし）',
    features: [
      { text: '当日の予想閲覧', included: true },
      { text: '過去の的中実績確認', included: true },
      { text: '買い目情報', included: false },
      { text: 'データ可視化', included: false },
      { text: '穴馬情報', included: false },
    ],
    cta: '無料で始める',
    ctaLink: '/free-prediction',
    highlighted: false,
  },
  {
    id: 'pro',
    name: 'プロ',
    price: 4980,
    yearlyPrice: 49800,
    period: '/月',
    description: '全レース馬単買い目',
    features: [
      { text: '全レース予想', included: true },
      { text: '馬単買い目提供', included: true },
      { text: '的中実績閲覧', included: true },
      { text: 'データ可視化', included: true },
      { text: '南関4場対応', included: true },
    ],
    cta: 'プロプランを始める',
    ctaLink: '#paypal-pro',
    highlighted: true,
    badge: 'おすすめ',
    dailyPrice: 166,
  },
];
};
---

<BaseLayout
  title="料金プラン"
  description="KEIBA Intelligenceの料金プラン。月額¥2,980から始められる低価格設定。フリープランから最上位アルティメットプランまで、あなたのニーズに合わせて選べます。"
>
  <!-- ヘッダーセクション -->
  <section class="pricing-header gradient-bg">
    <div class="container text-center">
      <h1 class="fade-in">料金プラン</h1>
      <p class="section-subtitle slide-in">
        低価格で始められる、明確な料金体系。<br>
        1日あたり¥99（缶コーヒー1本分）から本格的な競馬予想が利用可能。
      </p>
    </div>
  </section>

  <!-- メインプラン -->
  <section class="pricing-plans">
    <div class="container">
      <h2 class="section-title text-center">メインプラン</h2>

      <div class="pricing-toggle text-center mb-4">
        <button class="toggle-btn active" data-billing="monthly">月額払い</button>
        <button class="toggle-btn" data-billing="yearly">年額払い<span class="badge badge-success ml-1">2ヶ月無料</span></button>
      </div>

      <div class="grid grid-5">
        {plans.map((plan) => (
          <div class={`pricing-card card ${plan.highlighted ? 'featured' : ''}`}>
            {plan.badge && (
              <div class="badge badge-success">{plan.badge}</div>
            )}

            <div class="plan-header">
              <div class="plan-name">{plan.name}</div>
              <div class="plan-price">
                <span class="price monthly-price">¥{plan.price.toLocaleString()}</span>
                <span class="price yearly-price" style="display: none;">¥{plan.yearlyPrice.toLocaleString()}</span>
                <span class="period">{plan.period}</span>
              </div>
              {plan.dailyPrice && (
                <div class="daily-price monthly-price">1日あたり¥{plan.dailyPrice}</div>
              )}
              {plan.yearlyPrice > 0 && (
                <div class="yearly-savings yearly-price" style="display: none;">
                  年間¥{((plan.price * 12) - plan.yearlyPrice).toLocaleString()}お得
                </div>
              )}
              <div class="plan-description">{plan.description}</div>
            </div>

            <ul class="plan-features">
              {plan.features.map((feature) => (
                <li class={feature.included ? 'included' : 'excluded'}>
                  {feature.included ? '✅' : '❌'} {feature.text}
                </li>
              ))}
            </ul>

            <a href={plan.ctaLink} class="btn btn-primary w-full">
              {plan.cta}
            </a>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- プラン比較表 -->
  <section class="plan-comparison">
    <div class="container">
      <h2 class="section-title text-center">プラン機能比較表</h2>

      <div class="comparison-table-wrapper">
        <table class="comparison-table">
          <thead>
            <tr>
              <th>機能</th>
              <th>フリー</th>
              <th class="highlighted">プロ</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td>予想閲覧</td>
              <td>✅</td>
              <td>✅</td>
            </tr>
            <tr>
              <td>対象レース</td>
              <td>-</td>
              <td>全レース</td>
            </tr>
            <tr>
              <td>馬単買い目</td>
              <td>❌</td>
              <td>✅</td>
            </tr>
            <tr>
              <td>三連複買い目</td>
              <td>❌</td>
              <td>❌</td>
            </tr>
            <tr>
              <td>穴馬情報</td>
              <td>❌</td>
              <td>❌</td>
            </tr>
            <tr>
              <td>データ可視化</td>
              <td>❌</td>
              <td>✅</td>
            </tr>
            <tr>
              <td>南関4場対応</td>
              <td>❌</td>
              <td>✅</td>
            </tr>
            <tr>
              <td>サポート</td>
              <td>-</td>
              <td>標準</td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </section>

  <!-- FAQ -->
  <section class="pricing-faq">
    <div class="container">
      <h2 class="section-title text-center">よくある質問</h2>

      <div class="faq-grid">
        <div class="faq-item card">
          <h3>🤔 プランの変更は可能ですか？</h3>
          <p>
            はい、いつでも可能です。アップグレード・ダウングレードともに可能で、日割り計算で調整されます。
          </p>
        </div>

        <div class="faq-item card">
          <h3>💳 支払い方法は？</h3>
          <p>
            PayPal決済に対応しています。クレジットカード（Visa, Mastercard, JCB, Amex）またはPayPal残高からお支払いいただけます。
          </p>
        </div>

        <div class="faq-item card">
          <h3>🔄 解約はいつでもできますか？</h3>
          <p>
            はい、いつでも可能です。解約後も契約期間満了まではサービスをご利用いただけます。
          </p>
        </div>

        <div class="faq-item card">
          <h3>📊 的中率・回収率は？</h3>
          <p>
            過去360レースの実績で、的中率78.1%、回収率125.3%を記録しています。詳細は<a href="/results">的中実績ページ</a>をご確認ください。
          </p>
        </div>

        <div class="faq-item card">
          <h3>🏇 対象競馬場は？</h3>
          <p>
            南関東4競馬場（大井・川崎・船橋・浦和）に対応しています。将来的にJRA対応も予定しています。
          </p>
        </div>

        <div class="faq-item card">
          <h3>📱 スマホでも使えますか？</h3>
          <p>
            はい、完全レスポンシブ対応です。スマホ・タブレット・PCのいずれでも快適にご利用いただけます。
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="cta gradient-bg">
    <div class="container text-center">
      <h2>今すぐ始めて、勝ち馬を見つけよう</h2>
      <p class="mb-3">まずは無料プランで予想をチェック。納得したら有料プランで買い目をゲット。</p>
      <div class="cta-buttons">
        <a href="/free-prediction" class="btn btn-secondary btn-lg">無料で始める</a>
        <a href="#thrivecart-standard" class="btn btn-primary btn-lg">スタンダードプランを始める</a>
      </div>
    </div>
  </section>

  <style>
    /* ヘッダー */
    .pricing-header {
      padding: var(--spacing-2xl) 0;
      min-height: 300px;
      display: flex;
      align-items: center;
    }

    /* プラン切り替えトグル */
    .pricing-toggle {
      display: flex;
      justify-content: center;
      gap: var(--spacing-md);
      margin-bottom: var(--spacing-xl);
    }

    .toggle-btn {
      padding: var(--spacing-sm) var(--spacing-lg);
      border: 2px solid var(--border-color);
      background: var(--bg-secondary);
      color: var(--text-secondary);
      border-radius: var(--radius-md);
      cursor: pointer;
      font-weight: 600;
      transition: all var(--transition-base);
    }

    .toggle-btn.active {
      background: var(--primary-gradient);
      color: white;
      border-color: var(--primary-end);
    }

    .toggle-btn:hover {
      transform: translateY(-2px);
    }

    .ml-1 {
      margin-left: var(--spacing-xs);
    }

    /* プランカード（5カラムグリッド） */
    .pricing-plans {
      padding: var(--spacing-2xl) 0;
    }

    .grid-5 {
      display: grid;
      grid-template-columns: repeat(5, 1fr);
      gap: var(--spacing-lg);
    }

    .pricing-card {
      text-align: center;
      position: relative;
      display: flex;
      flex-direction: column;
    }

    .pricing-card.featured {
      border: 2px solid var(--primary-end);
      transform: scale(1.05);
    }

    .pricing-card .badge {
      position: absolute;
      top: var(--spacing-md);
      right: var(--spacing-md);
    }

    .plan-header {
      margin-bottom: var(--spacing-lg);
    }

    .plan-name {
      font-size: 1.5rem;
      font-weight: 700;
      margin-bottom: var(--spacing-md);
    }

    .plan-price {
      margin-bottom: var(--spacing-sm);
    }

    .price {
      font-size: 2.5rem;
      font-weight: 700;
      background: var(--primary-gradient);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .period {
      font-size: 1rem;
      color: var(--text-secondary);
    }

    .daily-price {
      font-size: 0.875rem;
      color: var(--text-tertiary);
      margin-bottom: var(--spacing-sm);
    }

    .yearly-savings {
      font-size: 0.875rem;
      color: var(--success);
      font-weight: 600;
      margin-bottom: var(--spacing-sm);
    }

    .plan-description {
      color: var(--text-secondary);
      margin-bottom: var(--spacing-md);
    }

    .plan-features {
      list-style: none;
      text-align: left;
      margin-bottom: var(--spacing-lg);
      flex-grow: 1;
    }

    .plan-features li {
      padding: var(--spacing-sm) 0;
      color: var(--text-secondary);
      font-size: 0.875rem;
    }

    .plan-features li.included {
      color: var(--text-primary);
    }

    /* AI Plusプラン */
    .ai-plus-plan {
      padding: var(--spacing-2xl) 0;
      background: var(--bg-secondary);
    }

    .ai-plus-card {
      max-width: 800px;
      margin: 0 auto;
      text-align: center;
    }

    .ai-plus-content {
      display: grid;
      grid-template-columns: 2fr 1fr;
      gap: var(--spacing-xl);
      margin-bottom: var(--spacing-lg);
      text-align: left;
    }

    .ai-plus-note {
      display: flex;
      align-items: center;
      color: var(--warning);
      font-size: 0.875rem;
      font-weight: 600;
      padding: var(--spacing-md);
      background: rgba(245, 158, 11, 0.1);
      border-radius: var(--radius-md);
      border: 1px solid var(--warning);
    }

    /* プラン比較表 */
    .plan-comparison {
      padding: var(--spacing-2xl) 0;
    }

    .comparison-table-wrapper {
      overflow-x: auto;
    }

    .comparison-table {
      width: 100%;
      border-collapse: collapse;
      background: var(--bg-card);
      border-radius: var(--radius-lg);
      overflow: hidden;
    }

    .comparison-table th,
    .comparison-table td {
      padding: var(--spacing-md);
      text-align: center;
      border-bottom: 1px solid var(--border-color);
    }

    .comparison-table th {
      background: var(--bg-secondary);
      font-weight: 700;
      color: var(--text-primary);
    }

    .comparison-table th.highlighted {
      background: var(--primary-gradient);
      color: white;
    }

    .comparison-table td:first-child {
      text-align: left;
      font-weight: 600;
    }

    /* FAQ */
    .pricing-faq {
      padding: var(--spacing-2xl) 0;
      background: var(--bg-secondary);
    }

    .faq-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: var(--spacing-lg);
    }

    .faq-item h3 {
      font-size: 1.125rem;
      margin-bottom: var(--spacing-sm);
    }

    .faq-item p {
      color: var(--text-secondary);
      font-size: 0.875rem;
      margin: 0;
    }

    /* CTA */
    .cta {
      padding: var(--spacing-2xl) 0;
      margin-top: var(--spacing-2xl);
    }

    .cta-buttons {
      display: flex;
      gap: var(--spacing-md);
      justify-content: center;
    }

    /* レスポンシブ */
    @media (max-width: 1280px) {
      .grid-5 {
        grid-template-columns: repeat(3, 1fr);
      }
    }

    @media (max-width: 1024px) {
      .grid-5 {
        grid-template-columns: repeat(2, 1fr);
      }

      .ai-plus-content {
        grid-template-columns: 1fr;
      }

      .faq-grid {
        grid-template-columns: 1fr;
      }
    }

    @media (max-width: 640px) {
      .grid-5 {
        grid-template-columns: 1fr;
      }

      .pricing-card.featured {
        transform: scale(1);
      }

      .price {
        font-size: 2rem;
      }

      .cta-buttons {
        flex-direction: column;
      }

      .pricing-toggle {
        flex-direction: column;
      }
    }
  </style>

  <script>
    // プラン切り替え（月額/年額）
    const toggleButtons = document.querySelectorAll('.toggle-btn');
    const monthlyPrices = document.querySelectorAll('.monthly-price');
    const yearlyPrices = document.querySelectorAll('.yearly-price');

    toggleButtons.forEach(button => {
      button.addEventListener('click', () => {
        const billing = button.dataset.billing;

        // ボタン状態更新
        toggleButtons.forEach(btn => btn.classList.remove('active'));
        button.classList.add('active');

        // 価格表示切り替え
        if (billing === 'monthly') {
          monthlyPrices.forEach(el => el.style.display = '');
          yearlyPrices.forEach(el => el.style.display = 'none');
        } else {
          monthlyPrices.forEach(el => el.style.display = 'none');
          yearlyPrices.forEach(el => el.style.display = '');
        }
      });
    });
  </script>
</BaseLayout>
