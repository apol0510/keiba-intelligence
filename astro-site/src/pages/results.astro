---
// 現在の年月を取得して月別アーカイブにリダイレクト
const now = new Date();
const currentYear = now.getFullYear();
const currentMonth = String(now.getMonth() + 1).padStart(2, '0');

return Astro.redirect(`/archive/${currentYear}/${currentMonth}/`);
---
