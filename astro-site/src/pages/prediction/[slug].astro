---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { readFileSync, existsSync } from 'fs';
import { join } from 'path';

export const prerender = false;

// URLからslugを取得（例: 2026-01-14-ooi）
const { slug } = Astro.params;

// JSONファイルパス
const jsonFilePath = join(process.cwd(), 'src', 'data', 'predictions', `${slug}.json`);

// エラー状態
let error = null;
let predictionData = null;

try {
  // ファイル存在確認
  if (!existsSync(jsonFilePath)) {
    throw new Error(`予想データが見つかりません: ${slug}`);
  }

  // JSONファイル読み込み
  const fileContent = readFileSync(jsonFilePath, 'utf-8');
  predictionData = JSON.parse(fileContent);

  // データ検証
  if (!predictionData.eventInfo || !predictionData.predictions) {
    throw new Error('予想データの形式が不正です');
  }
} catch (err) {
  error = err.message;
}

// ページタイトル生成
const pageTitle = predictionData
  ? `${predictionData.eventInfo.date} ${predictionData.eventInfo.venue} 全${predictionData.eventInfo.totalRaces}R予想`
  : '予想データ';
---

<BaseLayout title={pageTitle} description="AI予想データ">
  <section class="prediction-section">
    <div class="container">
      {error ? (
        <!-- エラー表示 -->
        <div class="card error-card">
          <h1>⚠️ エラー</h1>
          <p>{error}</p>
          <p class="help-text">
            URLを確認してください。形式: /prediction/2026-01-14-ooi
          </p>
          <a href="/" class="btn btn-primary">トップページに戻る</a>
        </div>
      ) : (
        <!-- 予想データ表示 -->
        <>
          <!-- ページヘッダー -->
          <div class="page-header">
            <h1 class="page-title">
              {predictionData.eventInfo.date} {predictionData.eventInfo.venue}
            </h1>
            <p class="page-subtitle">
              全{predictionData.eventInfo.totalRaces}R AI予想
            </p>
          </div>

          <!-- レース選択ボタン -->
          <div class="race-selector">
            {predictionData.predictions.map((race, index) => (
              <button
                class={`race-btn ${index === 0 ? 'active' : ''}`}
                data-race-index={index}
                onclick="selectRace(event, this)"
              >
                {race.raceInfo.raceNumber}R
              </button>
            ))}
          </div>

          <!-- レース表示エリア -->
          <div class="race-display-area">
            {predictionData.predictions.map((race, index) => {
              // 役割別馬リスト
              const honmei = race.horses.filter(h => h.role === '本命');
              const taikou = race.horses.filter(h => h.role === '対抗');
              const tanana = race.horses.filter(h => h.role === '単穴');
              const renka = race.horses.filter(h => h.role === '連下');
              const osae = race.horses.filter(h => h.role === '抑え');

              return (
                <div class={`race-content ${index === 0 ? 'active' : ''}`} data-race-index={index}>
                  <!-- 買い目 -->
                  <div class="betting-section">
                    <h2>🎯 買い目（馬単）</h2>
                    <div class="betting-lines">
                      {race.bettingLines.umatan.map((line, idx) => (
                        <div class="betting-line">
                          <span class="axis-label">{idx === 0 ? '本命軸' : '対抗軸'}</span>
                          <span class="betting-formula">{line}</span>
                        </div>
                      ))}
                    </div>
                  </div>

                  <!-- 出走馬 -->
                  <div class="horses-section">
                    <h2>🏇 出走馬</h2>

                    <!-- 本命・対抗・単穴 -->
                    <div class="main-horses">
                      {[...honmei, ...taikou, ...tanana].map((horse) => (
                        <div class={`horse-card ${horse.role === '本命' ? 'honmei' : horse.role === '対抗' ? 'taikou' : 'tanana'}`}>
                          <div class="horse-mark">
                            {horse.role === '本命' ? '◎' : horse.role === '対抗' ? '○' : '▲'}
                          </div>
                          <div class="horse-info">
                            <div class="horse-header-line">
                              <span class="horse-number">{horse.horseNumber}</span>
                              <span class="horse-name">{horse.horseName}</span>
                              <span class="role-badge">{horse.role}</span>
                            </div>
                          </div>
                        </div>
                      ))}
                    </div>

                    <!-- 連下 -->
                    {renka.length > 0 && (
                      <div class="sub-horses renka">
                        <div class="sub-header">△ 連下</div>
                        <div class="sub-list">
                          {renka.map((horse, idx, arr) => (
                            <span>
                              <strong>{horse.horseNumber}</strong> {horse.horseName}{idx < arr.length - 1 ? '、' : ''}
                            </span>
                          ))}
                        </div>
                      </div>
                    )}

                    <!-- 抑え -->
                    {osae.length > 0 && (
                      <div class="sub-horses osae">
                        <div class="sub-header">× 抑え</div>
                        <div class="sub-list">
                          {osae.map((horse, idx, arr) => (
                            <span>
                              <strong>{horse.horseNumber}</strong> {horse.horseName}{idx < arr.length - 1 ? '、' : ''}
                            </span>
                          ))}
                        </div>
                      </div>
                    )}
                  </div>
                </div>
              );
            })}
          </div>

          <!-- 注意事項 -->
          <div class="card info-card">
            <h3>📝 ご利用上の注意</h3>
            <ul>
              <li>この予想はAIによる分析結果であり、的中を保証するものではありません</li>
              <li>馬券の購入は自己責任でお願いします</li>
              <li>買い目は馬単（2段構成）で提供しています</li>
            </ul>
          </div>
        </>
      )}
    </div>
  </section>

  <style>
    .prediction-section {
      padding: var(--spacing-xl) 0;
      min-height: 100vh;
    }

    .page-header {
      text-align: center;
      margin-bottom: var(--spacing-xl);
      padding: var(--spacing-lg);
      background: linear-gradient(135deg, var(--primary-start), var(--primary-end));
      border-radius: var(--radius-lg);
    }

    .page-title {
      font-size: 1.75rem;
      margin-bottom: var(--spacing-sm);
      color: white;
    }

    .page-subtitle {
      font-size: 1.125rem;
      color: rgba(255, 255, 255, 0.9);
    }

    .error-card {
      text-align: center;
      padding: var(--spacing-2xl);
    }

    .error-card h1 {
      color: var(--error-color);
      margin-bottom: var(--spacing-lg);
    }

    .help-text {
      margin: var(--spacing-lg) 0;
      color: var(--text-secondary);
    }

    /* レース選択ボタン */
    .race-selector {
      display: flex;
      flex-wrap: wrap;
      gap: var(--spacing-sm);
      justify-content: center;
      margin-bottom: var(--spacing-xl);
      padding: var(--spacing-md);
      background: var(--bg-secondary);
      border-radius: var(--radius-lg);
    }

    .race-btn {
      padding: var(--spacing-sm) var(--spacing-lg);
      background: var(--bg-tertiary);
      color: var(--text-primary);
      border: 2px solid var(--border-color);
      border-radius: var(--radius-md);
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.2s;
    }

    .race-btn:hover {
      background: var(--primary-start);
      border-color: var(--primary-end);
      color: white;
    }

    .race-btn.active {
      background: linear-gradient(135deg, var(--primary-start), var(--primary-end));
      border-color: var(--primary-end);
      color: white;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
    }

    /* レース表示エリア */
    .race-display-area {
      position: relative;
      min-height: 500px;
    }

    .race-content {
      display: none;
    }

    .race-content.active {
      display: block;
      animation: fadeIn 0.3s ease-in-out;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* 買い目セクション */
    .betting-section {
      background: var(--bg-secondary);
      padding: var(--spacing-xl);
      border-radius: var(--radius-lg);
      margin-bottom: var(--spacing-xl);
      border-left: 4px solid #8b5cf6;
    }

    .betting-section h2 {
      font-size: 1.25rem;
      margin-bottom: var(--spacing-md);
    }

    .betting-lines {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-md);
    }

    .betting-line {
      display: flex;
      align-items: center;
      gap: var(--spacing-md);
      padding: var(--spacing-md);
      background: var(--bg-tertiary);
      border-radius: var(--radius-md);
    }

    .axis-label {
      font-weight: 600;
      color: var(--primary-end);
      min-width: 70px;
    }

    .betting-formula {
      font-family: 'Courier New', monospace;
      font-size: 1.25rem;
      font-weight: 700;
      color: var(--text-primary);
    }

    /* 出走馬セクション */
    .horses-section {
      background: var(--bg-secondary);
      padding: var(--spacing-xl);
      border-radius: var(--radius-lg);
      margin-bottom: var(--spacing-xl);
    }

    .horses-section h2 {
      font-size: 1.25rem;
      margin-bottom: var(--spacing-md);
    }

    /* 本命・対抗・単穴 */
    .main-horses {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-md);
      margin-bottom: var(--spacing-lg);
    }

    .horse-card {
      display: flex;
      align-items: center;
      gap: var(--spacing-md);
      padding: var(--spacing-md);
      background: var(--bg-tertiary);
      border-radius: var(--radius-md);
      border-left: 4px solid var(--border-color);
    }

    .horse-card.honmei {
      border-left-color: #ef4444;
      background: rgba(239, 68, 68, 0.1);
    }

    .horse-card.taikou {
      border-left-color: #3b82f6;
      background: rgba(59, 130, 246, 0.1);
    }

    .horse-card.tanana {
      border-left-color: #10b981;
      background: rgba(16, 185, 129, 0.1);
    }

    .horse-mark {
      font-size: 2rem;
      font-weight: 700;
      min-width: 40px;
      text-align: center;
    }

    .horse-card.honmei .horse-mark {
      color: #ef4444;
    }

    .horse-card.taikou .horse-mark {
      color: #3b82f6;
    }

    .horse-card.tanana .horse-mark {
      color: #10b981;
    }

    .horse-info {
      flex: 1;
    }

    .horse-header-line {
      display: flex;
      align-items: center;
      gap: var(--spacing-sm);
      flex-wrap: wrap;
    }

    .horse-number {
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--primary-end);
      min-width: 35px;
    }

    .horse-name {
      font-size: 1.125rem;
      font-weight: 600;
    }

    .role-badge {
      padding: 4px 10px;
      background: var(--primary-end);
      color: white;
      border-radius: var(--radius-sm);
      font-size: 0.75rem;
      font-weight: 600;
    }

    /* 連下・抑え */
    .sub-horses {
      margin-bottom: var(--spacing-md);
      padding: var(--spacing-md);
      background: var(--bg-tertiary);
      border-radius: var(--radius-md);
    }

    .sub-horses.renka {
      border-left: 4px solid #f59e0b;
    }

    .sub-horses.osae {
      border-left: 4px solid #6b7280;
    }

    .sub-header {
      font-weight: 600;
      margin-bottom: var(--spacing-sm);
    }

    .sub-list {
      line-height: 1.8;
      color: var(--text-secondary);
    }

    .sub-list strong {
      color: var(--text-primary);
    }

    /* 注意事項 */
    .info-card {
      max-width: 600px;
      margin: 0 auto;
      padding: var(--spacing-lg);
    }

    .info-card h3 {
      font-size: 1.125rem;
      margin-bottom: var(--spacing-md);
    }

    .info-card ul {
      list-style-position: inside;
      line-height: 1.8;
    }

    .info-card li {
      margin-bottom: var(--spacing-sm);
    }

    @media (max-width: 640px) {
      .page-title {
        font-size: 1.25rem;
      }

      .betting-formula {
        font-size: 1rem;
      }

      .horse-header-line {
        flex-direction: column;
        align-items: flex-start;
      }
    }
  </style>

  <script is:inline>
    function selectRace(event, button) {
      event.preventDefault();

      const raceIndex = button.getAttribute('data-race-index');

      // すべてのボタンからactiveを削除
      document.querySelectorAll('.race-btn').forEach(btn => {
        btn.classList.remove('active');
      });

      // クリックされたボタンをアクティブに
      button.classList.add('active');

      // すべてのレースコンテンツを非表示
      document.querySelectorAll('.race-content').forEach(content => {
        content.classList.remove('active');
      });

      // 選択されたレースを表示
      const selectedRace = document.querySelector(`.race-content[data-race-index="${raceIndex}"]`);
      if (selectedRace) {
        selectedRace.classList.add('active');
      }
    }
  </script>
</BaseLayout>
