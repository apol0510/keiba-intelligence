uat
恒久 UAT 環境の branch deploy を識別するためのマーカー。

正本: docs/UAT_PERMANENT_ENV.md

🔴 このファイルを main へ merge しない。
🟢 このファイルがあるのは、main とツリーが完全一致していると Netlify が
   "Canceled build due to no content change" でビルドをスキップしてしまうため。
   uat ブランチを main へ追随させたあとも、このファイルは残すこと。

--- 再デプロイ記録 ---
2026-09-12: branch:uat スコープの env 8 キーと Stripe Test の webhook 送信先を
反映させるための再デプロイ。Netlify は env を deploy 時に注入するため、
env を足しただけでは既存 deploy に反映されない。
🟢 空コミットだと "no content change" でスキップされるので、このファイルを更新して
   確実にビルドを走らせる。

2026-09-12(2): STRIPE_WEBHOOK_SECRET(branch:uat) の修正を反映するための再デプロイ。
Stripe の配信が 4 件すべて 400 invalid_signature で失敗しており、
署名シークレットの不一致が原因だった（コード側は isBase64Encoded を正しく処理しており不具合なし）。
🔴 env を直しただけでは反映されない。必ずこの再デプロイを通すこと。
