name: Import Horse Histories (Dispatch)

# admin の auto-fetch-horse-histories.mjs から送られる
# repository_dispatch (horse-histories-updated) を受け取り、
# keiba-data-shared/jra/horseHistories/... を本リポジトリの
# astro-site/src/data/horseHistories/jra/... へ転記する。
#
# 既存の archiveResultsJra.json 等とは独立し、ファイル単位で書き出すため
# 別パス・別 concurrency group で動かす。

on:
  repository_dispatch:
    types: [horse-histories-updated]
  workflow_dispatch:
    inputs:
      date:
        description: '日付 (YYYY-MM-DD)'
        required: false
        default: ''
      venues:
        description: 'カンマ区切りの場コード (例: TOK,KYO,NII)'
        required: false
        default: ''

# 並行実行制御：horseHistories ファイル群更新系で統一
concurrency:
  group: archive-horse-histories-update
  cancel-in-progress: false

jobs:
  import-horse-histories-jra:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # git push に必要
    defaults:
      run:
        working-directory: astro-site

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'astro-site/package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Import horseHistories from keiba-data-shared
        env:
          # keiba-data-shared 読み取り専用 PAT（KEIBA_DATA_SHARED_TOKEN 必須・GITHUB_TOKEN fallback 禁止）
          KEIBA_DATA_SHARED_TOKEN: ${{ secrets.KEIBA_DATA_SHARED_TOKEN }}
          MANUAL_DATE: ${{ github.event.inputs.date }}
          MANUAL_VENUES: ${{ github.event.inputs.venues }}
          PAYLOAD_DATE: ${{ github.event.client_payload.date }}
          PAYLOAD_VENUES_JSON: ${{ toJSON(github.event.client_payload.venues) }}
          PAYLOAD_PATHS_JSON: ${{ toJSON(github.event.client_payload.paths) }}
        run: |
          # 優先順位: 手動入力 > repository_dispatch payload
          if [ -n "$MANUAL_DATE" ]; then
            echo "📅 Manual horseHistories import for: $MANUAL_DATE"
            if [ -n "$MANUAL_VENUES" ]; then
              npm run import:horse-histories:jra -- --date "$MANUAL_DATE" --venues "$MANUAL_VENUES"
            else
              npm run import:horse-histories:jra -- --date "$MANUAL_DATE"
            fi
          elif [ -n "$PAYLOAD_DATE" ] && [ "$PAYLOAD_DATE" != "null" ]; then
            echo "📅 Importing horseHistories for: $PAYLOAD_DATE"
            # venues は payload から JSON 配列で来るのでカンマ区切りに変換
            VENUES_CSV=$(echo "$PAYLOAD_VENUES_JSON" | jq -r 'if . == null then "" else join(",") end')
            if [ -n "$VENUES_CSV" ]; then
              npm run import:horse-histories:jra -- --date "$PAYLOAD_DATE" --venues "$VENUES_CSV"
            else
              npm run import:horse-histories:jra -- --date "$PAYLOAD_DATE"
            fi
          else
            echo "❌ No date provided (neither manual input nor client_payload.date)"
            exit 1
          fi

      - name: Check for changes
        id: git-check
        run: |
          # horseHistories 配下の変更だけをステージング
          git add src/data/horseHistories/
          git diff --staged --exit-code || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push changes
        if: steps.git-check.outputs.changed == 'true'
        env:
          MANUAL_DATE: ${{ github.event.inputs.date }}
          PAYLOAD_DATE: ${{ github.event.client_payload.date }}
          PAYLOAD_VENUES_JSON: ${{ toJSON(github.event.client_payload.venues) }}
          SOURCE: ${{ github.event.client_payload.source }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          if [ -n "$MANUAL_DATE" ]; then
            DATE_FOR_MSG="$MANUAL_DATE"
            VENUES_FOR_MSG="(manual)"
          else
            DATE_FOR_MSG="$PAYLOAD_DATE"
            VENUES_FOR_MSG=$(echo "$PAYLOAD_VENUES_JSON" | jq -r 'if . == null then "" else join(",") end')
          fi
          COMMIT_MSG="Auto-import: ${DATE_FOR_MSG} horseHistories [${VENUES_FOR_MSG}] from ${SOURCE:-jra-official}"

          # horseHistories ファイル群を退避
          mkdir -p /tmp/hh-backup
          cp -r src/data/horseHistories /tmp/hh-backup/
          echo "📦 Backed up horseHistories"

          # Retry: reset → restore → commit → push（最大5回）
          MAX_RETRIES=5
          RETRY_COUNT=0
          SUCCESS=false
          while [ $RETRY_COUNT -lt $MAX_RETRIES ] && [ "$SUCCESS" = false ]; do
            echo "🔄 Attempt $((RETRY_COUNT + 1))/$MAX_RETRIES"

            git fetch origin main
            git reset --hard origin/main
            git clean -fd

            # 退避した horseHistories を復元
            mkdir -p src/data
            cp -r /tmp/hh-backup/horseHistories src/data/

            git add src/data/horseHistories/
            if git diff --staged --quiet; then
              echo "⏭️  No changes after sync (remote already has latest)"
              SUCCESS=true
              break
            fi
            git commit -m "$COMMIT_MSG"

            if git push origin main; then
              echo "✅ Successfully pushed horseHistories"
              SUCCESS=true
            else
              echo "⚠️  Push failed (remote updated during attempt)"
              RETRY_COUNT=$((RETRY_COUNT + 1))
              sleep 2
            fi
          done

          if [ "$SUCCESS" = false ]; then
            echo "❌ Failed to push after $MAX_RETRIES attempts"
            exit 1
          fi

      - name: No changes notification
        if: steps.git-check.outputs.changed != 'true'
        run: |
          echo "⏭️  No changes detected (horseHistories already up-to-date)"
